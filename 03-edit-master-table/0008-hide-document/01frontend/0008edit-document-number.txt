**AI Generation Specification Prompt**

**[1. PAGE TITLE]**
Hide Document Number — Chememan

**[2. PURPOSE]**
Provide a secure compliance and audit control panel to conceal or mask specific SAP Document Numbers from view during precise Fiscal Periods (Year + Month combinations). This tool allows controllers to freeze visibility during closed reporting cycles, internal audits, or compliance adjustments.

**[3. DESIGN SYSTEM]**
- **Semantic Component Identifiers:**
  * Document Numbers use clean mint layouts: `--gl-code: #1FA378`.
  * Fiscal Closing Periods utilize warm gold accents: `--gl-group: #C9963D`.
- **Text & UI Rules:** Heavy reliance on `--font-mono` for data elements, bold layout metrics labels, clean grey border lines, and smooth modal fade-in timelines. Full dark theme compatibility.

**[4. LAYOUT STRUCTURE]**
- **Standard Navigation Banner:** Top branding layout with page details and active theme toggle switches.
- **Quad-Field Inline Entry Form Bar (`form-grid-4`):** A streamlined horizontal input form dividing workspace fields into 4 columns:
  1. Document Number search selection dropdown box.
  2. Fiscal Year numeric selector field.
  3. Fiscal Month dropdown component.
  4. Primary "Save/Record Rule" submission action button.
- **Search & Counter Summary Control Row:** A utility section providing text-based filter fields and dynamic records counter pills.
- **Rule Exclusion Matrix Table:** Data list layout containing tracking columns for SAP Document ID, Target Covered Period (`YYYY-MM`), rule creation timestamp, and removal action toggles.
- **Interactive Warning Overlays:** Dialog boxes managing validation notices and action confirmations.

**[5. INTERACTIVE COMPONENTS & BEHAVIORS]**
- **Document Lookup Filter Dropdown:** Typing filter applied onto the SAP Document Number input, handling dropdown visibility state, matching strings against system data repositories, and autofilling inputs on item select.
- **Compound Primary Key Validation:** Combines individual Fiscal Year and Month inputs into an unified string key (`YYYY-MM`). The system scans the database before addition to avoid duplicate exclusion rule mappings.
- **Interactive Workspace Query Filter:** Active string filter attached to the table search bar that parses rules and hides non-matching table rows instantly.
- **Dynamic Array Splicing (CRUD):** Complete state pipeline facilitating new record mapping additions, cell modifications, and immediate rule deletion with live table row re-rendering and dynamic counter updates.