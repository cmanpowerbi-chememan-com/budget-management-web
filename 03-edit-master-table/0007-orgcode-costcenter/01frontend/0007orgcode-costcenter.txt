**AI Generation Specification Prompt**

**[1. PAGE TITLE]**
Orgcode & Cost center Master — Chememan

**[2. PURPOSE]**
Provide an administrative mapping dashboard to control the corporate relational links between Cost Centers (manually keyed parameters representing business sites) and SAP Orgcode identifiers (representing business division entities). It provides clean visualizations of corporate hierarchies and unmapped nodes.

**[3. DESIGN SYSTEM]**
- **Accent Identity Markers:**
  * Manually managed Cost Centers: Highlighted via custom blue/slate badges (`--gl-group: #1B7BA8`).
  * System SAP Orgcode entities: Highlighted via standard mint tags (`--gl-code: #1FA378`).
- **Visual Stylings:** Soft cream backgrounds matching `--paper`, elegant line separators, card hover elevation changes, and sharp contrast text components. Fully themeable between light and dark modes.

**[4. LAYOUT STRUCTURE]**
- **Corporate Global Header:** Top navigation panel layout housing breadcrumbs, application title, and light/dark theme switchers.
- **Relational Master-Detail Workspace:** Split design optimized for bulk coordination:
  * **Configuration Panel (Left Card):** Simple mapping input fields for typing Cost Center values and selecting SAP Orgcode strings via search dropdowns.
  * **Relational Mapping Matrix Grid (Right Panel):** A highly distinct layout presenting data records. It summarizes total unique active Cost Centers and Orgcodes via live legend counts, and renders mapping structures.
- **Relational Cards Table:** Features searchable data layouts displaying mapping pairs, summary counts, and standard record modification tools.

**[5. INTERACTIVE COMPONENTS & BEHAVIORS]**
- **Real-Time Input Regex Validation:** The Cost Center text input handles data filtering on input (`oninput`). It screens characters using `/[^0-9A-Za-z]/g`, automatically transforming entries to uppercase. If a user types illegal characters, the field flashes a warning red border and shadow box effect that auto-clears after 600ms.
- **Live Search List Dropdown:** Search input box for SAP Orgcodes utilizing fuzzy lookup matching against backend data models, with auto-dropdown placement and focus management.
- **State Relation Map Compilation:** Custom rendering function (`renderTable`) that creates Map hashes (`ccMap`, `orgMap`) at runtime to group relations and dynamically calculate how many distinct business units are mapped, updating legend UI chips automatically.
- **CRUD Update Engine:** Handles active records injection, entry additions, mapping deletion, form switching, and feedback notifications.