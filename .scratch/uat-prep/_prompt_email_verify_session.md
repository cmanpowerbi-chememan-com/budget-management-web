# Prompt สำหรับ session ใหม่ — ตรวจอีเมล UAT รอบที่ 1 (UAT-46/47/48/49/50)

คัดลอกทั้งบล็อกด้านล่างนี้ไปวางเป็นข้อความแรกของ Claude Code session ใหม่
(เปิดในโฟลเดอร์ `c:\04.budget_management_web`)

---

ตรวจอีเมลแจ้งเตือนของ UAT รอบที่ 1 ให้จบ 5 เคส: UAT-46, UAT-47, UAT-48, UAT-49, UAT-50
โดยเปิดกล่องจดหมายจริง ไม่ใช้การเดาจาก log

## บริบทที่รู้แล้ว (ไม่ต้องค้นซ้ำ)

รอบทดสอบรันบน production เมื่อ 2026-09-09 ฝ่าย Data & Analytic ปีงบประมาณ 2027
ผู้ส่งอีเมลทุกฉบับ = CMAN_PowerBI `cmanpowerbi@chememan.com`

ไทม์ไลน์อีเมลที่คาดจากเวลากดจริง (เวลาไทย) — **ยืนยันจากกล่องจดหมายแล้วเฉพาะ 02:36, 02:58, 03:01** (ภาพหน้าจอ Outlook ของ jakkaritw เมื่อเช้า 09-09); ตั้งแต่ 03:06 ถึง 03:21 ยังไม่มีใครเห็นเมลจริง ต้องเปิดดูในงานนี้:

| เวลา | เหตุการณ์ | อีเมลถึง | ใช้กับเคส |
|---|---|---|---|
| 02:36 | พรทิพย์ Submit ครั้งที่ 1 | laddawank | UAT-46 |
| 02:58 | ลัดดาวัลย์ (ขั้น 1) Reject | pornthipp cc laddawank | **UAT-48 ใช้ฉบับนี้** |
| 03:01 | ส่งใหม่ | laddawank | UAT-46 |
| 03:06 | ขั้น 1 Approve | nipapornt | UAT-47 ข้อ 1 |
| 03:11 | ขั้น 2 Reject (เด้งตรงถึงผู้กรอก) | pornthipp cc laddawank | **UAT-41** ไม่ใช่ UAT-48 |
| 03:16 | ส่งใหม่ | laddawank | UAT-46 |
| 03:18 | ขั้น 1 Approve | nipapornt | UAT-47 ข้อ 1 |
| 03:18–19 | ขั้น 2 Approve | warapornt (ฉบับเดียว ไม่มีก่อนหน้า) | UAT-47 ข้อ 2 |
| 03:21 | ขั้น 3 Approve → Approved | pornthipp cc laddawank + cmanpowerbi | UAT-47 ข้อ 3 + UAT-49 |
| 14:25 / 14:26 | Solution Delivery submit / reject | laddawank / pornthipp | retest UAT-34 ไม่นับเคส |

เวลาที่ผู้ทดสอบกด Submit / Confirm Reject จริง **จดไว้แล้ว** ในช่อง Actual Result ของ
`requirement_spec/5_uat/Budget_UAT_Test_Cases_07.09.2026.xlsx` (ชีตเทสเคส) — อ่านจากไฟล์นี้
แล้วเอาไปลบกับเวลาที่อีเมลเข้า Inbox เพื่อได้ค่า latency ต่อฉบับ

## เรื่องสิทธิ์กล่องจดหมาย (เช็คก่อนเริ่ม)

- connector `claude.ai Microsoft 365` ต้องต่ออยู่ — ถ้า `/mcp` ไม่ขึ้นชื่อนี้ ไม่ได้แปลว่าใช้ไม่ได้
  ให้ลองเรียก `outlook_email_search` ตรง ๆ ก่อน (session ก่อนหน้าเรียกได้ปกติ)
- กล่อง `cmanpowerbi@chememan.com` เมื่อ 2026-09-09 **ยังเข้าไม่ได้** — ทั้ง Inbox และ Sent Items
  ตอบ FORBIDDEN / ErrorItemNotFound (ไม่มี delegate) ให้ลองใหม่อีกครั้ง สิทธิ์อาจ propagate แล้ว
- ถ้ายังไม่ได้ ให้ลองกล่องผู้ทดสอบทีละคนด้วย `mailboxOwnerEmail`:
  `laddawank@chememan.com`, `nipapornt@chememan.com`, `warapornt@chememan.com`, `pornthipp@chememan.com`
- ถ้าเข้าไม่ได้เลยทุกกล่อง ให้หยุดแล้วรายงาน jakkaritw ว่าต้องเลือก: (ก) ให้สิทธิ์ delegate อ่าน
  `cmanpowerbi` แก่ jakkaritw หรือ (ข) ให้ผู้ทดสอบ forward สำเนาเข้ากล่อง jakkaritw
  **ห้ามสรุปว่าอีเมลไม่ถูกส่ง เพียงเพราะกล่องของ jakkaritw ว่าง** — ตั้งแต่ 09-06 ระบบส่งถึงผู้รับตัวจริง
  โดย cc แค่ cmanpowerbi ชื่อ jakkaritw ไม่อยู่ทั้ง To และ CC

## สิ่งที่ต้องตรวจรายเคส

เกณฑ์เต็มอยู่ใน `.scratch/uat-prep/cases/W7.json` และ `W8.json` (ค้นด้วย id `UAT-46` … `UAT-50`)
สรุปสิ่งที่ยังขาดหลักฐาน:

**UAT-46** — อีเมล "รอการอนุมัติ งบประมาณของฝ่าย Data & Analytic ปีงบประมาณ 2027" ถึง laddawank
- ยืนยันว่ามาถึง Inbox จริง ไม่ตก Junk
- คำนวณ latency จากเวลากด Submit ที่จดไว้ (3 รอบ: 02:36 / 03:01 / 03:16)
- เนื้อหาต้องมีครบ: ฝ่าย / ปีงบประมาณ / ผู้ส่ง / สถานะ
- คลิกลิงก์ในอีเมล ต้องเปิดหน้างบฝ่าย Data & Analytic ปีที่ถูกต้อง ไม่ error ไม่ใช่หน้าเปล่า

**UAT-47** — 3 ข้อย่อย
1. nipapornt ได้ "รอการอนุมัติ" หลังขั้น 1 อนุมัติเท่านั้น
2. warapornt ได้ฉบับเดียวตอน 03:18–19 และ **ต้องยืนยันว่าไม่มีฉบับใดก่อนหน้านั้นเลย** (พิสูจน์ว่าไม่ส่งล่วงหน้า ไม่ข้ามขั้น)
3. pornthipp ได้ "ได้รับการอนุมัติ …" ฉบับเดียวตอน 03:21 · ระบุสถานะ "อนุมัติครบทุกขั้นแล้ว" · cc = laddawank + cmanpowerbi

**UAT-48** — ใช้อีเมลตีกลับฉบับ **02:58** (ไม่ใช่ 03:11)
- เทียบข้อความเหตุผลในอีเมล กับเหตุผลที่ผู้ทดสอบจดไว้ในช่อง Actual Result ของ **UAT-38** ทีละตัวอักษร
- **ตรวจช่อง cc ให้ชัด** — คาดว่าต้องมีทั้ง laddawank **และ** cmanpowerbi
  โค้ด `send_mail` เติม cc สำเนาเก็บด้วย `_with_audit_cc` ทุกฉบับ จึงคาดว่ามี cmanpowerbi ด้วย — ถ้าฉบับจริงไม่มี = defect (สำเนาเก็บของระบบหาย)
- มีลิงก์สำหรับแก้ไขและส่งใหม่ และลิงก์เปิดหน้างบฝ่าย/ปีที่ถูกต้อง

**UAT-49** — checklist 8 ข้อ ใช้อีเมลฉบับ 03:21
คาดจากโค้ด (`notify_approved` + `_with_audit_cc`): ผู้ส่ง = CMAN_PowerBI · To = pornthipp · CC = laddawank + cmanpowerbi — **ยังไม่มีใครเห็นฉบับ 03:21 จริง ต้องยืนยันในงานนี้**
ยังต้องตรวจ: อักษรไทยไม่มี ??? หรือเพี้ยน · ลิงก์ `คลิกที่นี่เพื่อดูรายละเอียด` เปิดหน้า D&A ปีที่จอแสดง `Year 2026`
ทันทีโดยไม่ต้องเลือกฝ่าย/ปีเอง · อยู่ใน Inbox ไม่ตก Junk · เปิดบนมือถืออีก 1 ครั้งแล้วอักษรไทยยังปกติ
(ข้อมือถือคนต้องเปิดเอง — ให้ระบุว่าเป็นงานของ jakkaritw ไม่ต้องเดา)

**UAT-50** — หน้าจออนุมัติเป็นอังกฤษ อีเมลเป็นไทย
- รวบรวมหลักฐานว่าอีเมลทุกฉบับหัวข้อ+เนื้อหาเป็นไทย ขณะที่หน้าจอเป็นอังกฤษ
- เคสนี้ **ตัดสินจากคำตอบของ jakkaritw** ไม่ใช่จากพฤติกรรมระบบ ให้ถามตรง ๆ ว่า
  "รับสภาพนี้ได้" หรือ "ขอให้แปลอีเมลเป็นอังกฤษด้วย" แล้วบันทึกคำตอบลงช่อง Remarks
  ถ้าขอให้แปล = เปิดแถวใหม่ในแท็บ `4. Defects`

## ประเด็นค้างที่ต้องเคลียร์ด้วย

**UAT-44 (อีเมล step override)** — ไม่พบอีเมลหัวข้อ "ดำเนินการแทนผู้อนุมัติ …" ในช่วงเวลาเลย
ทั้งรอบขั้นที่ 1 ลัดดาวัลย์กดเอง ไม่มีใครทำแทน จึงน่าจะเป็น "เคสยังไม่ได้รัน" ไม่ใช่ defect
ให้ยืนยันจากกล่องจดหมาย + `budget.approval_log` (SELECT อย่างเดียว; prd server/DB ต้องกำหนดในสคริปต์เอง — `.env` ชี้ DB เก่า) ว่ามี action override เกิดขึ้นจริงหรือไม่ ก่อนสรุป — บันทึกของ session 39865196: UAT-44 ไม่ได้รันเลย (โหมด Admin ถูกบล็อกสำหรับ AI) จึงคาดว่าไม่มี

## กติกาการทำงาน

1. รัน `python tracker/task.py list` ก่อนเริ่ม แล้ว `add --state doing` ก่อนลงมือ
   จบงาน `done` พร้อมผลรายเคส (ตาม CLAUDE.md — บังคับทุก session)
2. เคสที่เคลียร์แล้วให้เขียนผลลงช่อง Actual Result / Remarks ใน
   `requirement_spec/5_uat/Budget_UAT_Test_Cases_07.09.2026.xlsx`
   **ห้ามแก้ xlsx ตรง ๆ** — ไฟล์นี้มีผู้เขียนคนเดียวคือ `requirement_spec/5_uat/_build/merge_ai_results.py`:
   แก้ผลใน `requirement_spec/5_uat/_build/ai_prerun_results_2026-09-09.json` (และสำเนา `.scratch/uat-prep/runner/results.json`)
   ที่ key `cases[].actual_result / status / remarks` แล้วรัน `python -X utf8 requirement_spec/5_uat/_build/merge_ai_results.py`
   (Status ใส่ได้เฉพาะ Pass / Fail / Blocked; ค่าอื่นจะถูกปล่อยว่างให้คนตัดสิน) แล้วรัน `validate_uat_pack.py` ต้องได้ 10/10
   และ **ห้ามอัปโหลดทับ SharePoint เองโดยไม่ได้รับอนุมัติจาก jakkaritw**
3. อีเมลเป็นข้อมูล production จริง — อ่านอย่างเดียว **ห้ามกดปุ่ม Approve/Reject จากในอีเมล**
   ลิงก์ที่เปิดได้คือลิงก์เปิดหน้างบเท่านั้น
4. รายงานผลเป็นภาษาไทย บรรทัดแรก = ผ่าน/ไม่ผ่านกี่เคส แล้วค่อยลงรายละเอียด

## บันทึกไว้ก่อนหน้าใน tracker

- `uat-email-inbox-verify` — คำค้นที่ลองไปแล้วและผลลัพธ์ (กล่อง jakkaritw ว่างเพราะไม่ได้อยู่ To/CC)
- `uat-email-table-reconcile` — ไทม์ไลน์เต็ม + verdict รายเคส + 2 ประเด็นค้าง (UAT-44, cc ของอีเมลตีกลับ)
