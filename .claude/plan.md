# Current Phase (2026-07-20)

## Grid "ซ่อนคอลัมน์" (compact mode) column toggle (2026-07-21, frontend only)
- [x] **Status-header button collapses GL Group/Remark/Status in BOTH side tables (COST+SGA) at once, leaving CC/GL Code/12 months + trailing ลบ column** — built ON TOP of the in-flight uncommitted work (Status = 5th frozen column `frz-5`). `frontend/src/grid/model.ts`: 3 new pure colSpan helpers (`identityColSpan` 4/2, `fullRowColSpan` 18/15, `subtotalLabelColSpan` 5/2) driving the group-head row / txn rows 2-3 / message row / filtered-empty row / subtotal label. `frontend/src/grid/GridTable.tsx`: single `columnsCollapsed` useState (NOT persisted — plain state, always expanded on load per jakkaritw's approved policy); collapse button (chevron-left double, `data-testid=collapse-columns-btn`) lives on the Status th, expand button (chevron-right double, `expand-columns-btn`) on the GL Code th, rendered only in the state where the other's th doesn't exist — no collision. Special-GL subform access in compact mode: the `open-subform-${cc}-${gl}` button moves from the (now-hidden) Pending status cell into a small icon-only "up-right arrow" button in the GL cell (row 1), same `onOpenSpecial` callback + same `meta.is_special && row.editable && onOpenSpecial` gate — the read-only `.special-hint` text is never rendered in compact mode (state-driven, no extra code needed since its parent status td doesn't render). Column filters (`colFilters` state + `filterRows`) are UNCHANGED — a filter typed into a now-hidden column (e.g. GL Group) stays applied while collapsed, by design. `frontend/src/styles/global.css`: new `.col-toggle-btn` (quiet 18x18 icon button, absolutely positioned inside the sticky th so it never grows header height) + `.special-open-btn.compact` (icon-only amber pill, same look as the existing expanded button). TDD RED-first: 12 new tests (3 `model.test.ts` + 9 `GridTable.test.tsx` — button presence/swap in both states, both-sides-collapse-together, special-GL editable/non-editable in compact, Pending inputs stay editable+commit, glGroup filter survives collapse, subtotal label survives). Full frontend suite 457->469 vitest pass, `npx tsc -b --noEmit` clean, `npx oxlint` clean on touched files, `npm run build` clean. **Deviation from spec**: none — implementation follows the GATE-approved design verbatim. **Not verified live (Playwright)** per this task's explicit instruction (logic-only feature, no screenshot required). Not committed, tracker NOT touched (per this task's explicit instructions — another session owns those files right now).

## Grid trailing "ลบ" (delete) column for web-added transactions (2026-07-20)
- [x] **`DELETE /budget/rows` + grid trailing Actions column, done 2026-07-20**: eligibility (jakkaritw-approved, 2 policy decisions) = `row.editable` AND no SAP value in any month AND no Approved value in any month AND `gl_group !== 'Travelling Expense'` (the other 5 special groups ARE deletable, backend cascades their detail lines; Travelling Expense stays owned by Trip Manager). Backend: `backend/app/write_model.py` `RowDeleteResult`/`_delete_one_pending_row`/`delete_pending_row` (same guard chain as `_delete_one_detail_line`/`_delete_one_trip`: not-excluded → write-scope-or-admin → department-not-locked → not-post-deadline; cascades `pending_budget_detail` then deletes the `pending_budget` row with the optimistic `_updated_at` lock; commit AFTER both deletes) + `backend/app/routers/budget_write.py` `DELETE /budget/rows`. Frontend: `frontend/src/grid/model.ts` `isDeletableRow`; `frontend/src/api/budget.ts` `deleteRow`; `frontend/src/grid/GridTable.tsx` trailing action column (colgroup `.action-col`, group-head-row + col-row trailing `th.action-col-head`, `TxnBlock`'s first (SAP) `<tr>` gets a `rowSpan={3}` `.action-cell` with the trash button — rows 2/3 add no `<td>`, message/empty-filtered `colSpan` 17→18, `SubtotalRow` gets a trailing empty `.action-cell`) + `frontend/src/grid/BudgetGrid.tsx` `handleDeleteRow` (Thai `window.confirm`, optimistic local removal, 409 → `loadGrid()` refetch) + `frontend/src/styles/global.css` `.action-col`/`.action-cell`/`.action-btn` (mockup `0002.3budget-export.html` lines 579-587 + trash SVG at line 2263, gridgeist quiet-recessive: 1px border, no fill until hover, one destructive accent `#C25A3F`) — also excluded `.action-cell` from the point-2 row-tint wash (same reasoning as `.status-cell`, since it's a `rowSpan` shared control). **Tests**: backend 16 new (9 `test_write_model.py` + 5 `test_budget_write.py` + 1 `test_conn_open_502.py` entry ×2 failure-modes) — full mocked suite 590→605 passed (pristine-baseline-verified via `git stash`); frontend 90 new/updated (`model.test.ts`+6, `GridTable.test.tsx`+6, `BudgetGrid.test.tsx`+3, `api/budget.test.ts`+2) — full suite 453/453 green, `tsc -b`/`npm run build`/`oxlint` all clean. Playwright headless verify (mocked routes, real Chromium, live dev server): 11/11 PASS (eligibility gate for 3 row shapes, DELETE payload carries all 4 params, txn-block removed from DOM on success, 0 console errors), screenshot `%TEMP%\delete_column_verify.png` — awaiting jakkaritw visual review. **Flagged, not a regression**: the new `test_delete_row_401_without_auth` router test joined the ALREADY-documented pre-existing "19 401-without-auth tests flake on this dev machine" issue (local `.env` `APP_ENV=local`+`DEV_AUTH_EMAIL` + process-wide `get_settings()` `@lru_cache` + test-run order) — confirmed via `git stash` that the exact same class of failure exists on pristine `main` with byte-identical test count delta; not fixed (out of this task's scope, same as every prior session that hit it). Not committed (per instructions).

## Trip Manager — consolidated save-all (2026-07-19, frontend only)
- [x] **Replaced per-card "บันทึกทริป" + per-row "บันทึก" buttons with ONE bottom-bar "บันทึก & ลงบัญชี"** in `frontend/src/subform/TripManager.tsx`: `saveAll()` loops a click-time snapshot of `cards`, threads each trip's `createTrip`/`updateTrip` RESPONSE (`trip_id`/`side`) directly into that same card's manual-line `saveDetailLine` calls — `cards` React state is never read again after an await, closing a stale-closure drop that would have silently skipped a new trip's manual expense line (financial never-cut, caught by a dedicated regression test). Every card in the batch is attempted independently (validation/trip/line failures never block siblings); `nextCards` is seeded from the snapshot and replaced by index so an unexpected throw can't drop a card, wrapped in `try/finally{setSaving(false)}` so the modal can never freeze. New `shouldWriteManualLine` (`model.ts`) encodes the write rule: a never-persisted all-zero line is skipped (no spurious row), an existing line edited down to zero IS written (server must zero it). `<fieldset disabled={saving}>` wraps all cards during a batch (native disable-everything semantics); footer now `[ยกเลิก(confirm-on-dirty)] [+ เพิ่มทริป] [บันทึก & ลงบัญชี]`; a batch 409 shows a Thai banner + explicit "โหลดข้อมูลล่าสุด" button (no auto-reload — would discard other cards' unsaved edits) and per-card/per-line error text replaces the removed buttons. Expense table now renders for unsaved trips too (Option B) — one click creates the trip AND saves its manual lines; GL chips show "—" until a side is picked. 20 new/updated tests in `TripManager.test.tsx` (122/122 subform, 422/422 full frontend suite green), `npx tsc -b` + `npm run build` clean, `npx oxlint` clean on touched files. Fixed 3 lines of PRE-EXISTING drift in the committed e2e spec `frontend/e2e/filler-journey.spec.ts` test 1.8 that the button removal broke (stale per-diem-note text + month-button labels from an earlier unrelated restyle, `save-trip-new-0`→`save-all`) — 20/23 e2e green; the 3 remaining failures (admin-toggle timeout, no-scope departmentsQueries count, deep-link year-picker aria-label) are in `admin-journey`/`edge-states`/`filler-journey 1.1`, pre-existing and unrelated (grid/year-picker area, out of this task's scope — confirmed by inspecting each failure's stack, none touch `TripManager`/`subform`). Not committed (per instructions).
- [x] **Delete-409 conflict guard fixed (2026-07-19)** — `deleteTripCard`'s 409 catch branch used to call `await load()` unconditionally, which replaces the whole `cards` array from the server and would SILENTLY discard any OTHER card's unsaved edits (a dirty header, or a manual line cleared to 0 but not yet saved — the cleared line would revert to its stale nonzero server value with no warning; financial never-cut). Fix: before reloading, check `cards.some(c => c.localId !== deletedId && (c.dirty || manualDirty[type]))`. If any OTHER card has unsaved edits → do NOT auto-load; reuse the SAME `conflictMessage` banner + "โหลดข้อมูลล่าสุด" button save-all's batch-conflict path already renders (no parallel mechanism invented), and mark the deleted card `status='error'` with the conflict message (stays visible, never silently wiped). If NO other card has unsaved edits → kept the original auto-load (simplest-consistent choice — nothing to lose). 2 new/updated tests in `TripManager.test.tsx` (123/123 subform, 425/425 full frontend suite green): the new guard test (2 cards, card B's persisted line cleared to 0, delete card A → 409 → `fetchTrips` called only once, banner+button shown, card B's cleared input stays empty, clicking reload calls `fetchTrips` a 2nd time) + the pre-existing single-card 409 test relabeled as the "no other unsaved edits, safe to auto-reload" regression case. `npx tsc -b` + `npm run build` clean. `frontend/src/grid/**` untouched. Not committed (per instructions).

## GL master-membership visibility rule (2026-07-19, product decision by jakkaritw)
- [x] **Enforce: a GL not in `dbo.gl_group` is HIDDEN from the budget grid entirely** (reverses the earlier "add-later reference" behavior — no more read-only reference row). `backend/app/gl_access.py` `fetch_master_gl_codes(conn) -> frozenset[str]` (one `SELECT gl_code FROM dbo.gl_group`) + `backend/app/read_model.py` `merge_budget_rows(..., master_gl_codes=None)` drops any row whose gl not in the set, composes independently with the existing role-gated `admin_gl_codes` strip; `get_budget_grid` always fetches it (NOT flag-gated, NOT role-based — applies to admin and non-admin alike, unlike the `edit_by` admin-lock). Frontend: removed the now-dead `NOT_IN_MASTER_HINT`/`.reference-hint` render branch in `frontend/src/grid/GridTable.tsx` + its CSS in `global.css` (kept `GlMeta.in_master`/`isEditableCell`'s `glInMaster` guard as harmless defense-in-depth — untouched special-GL/editable logic). 24 new backend unit tests (14 `test_read_model.py` + 4+4 `test_gl_access.py`) green, 401/401 frontend tests green (3 `GridTable.test.tsx` cases updated for the new UI, no assertions weakened), `npm run build` green. Live-verified (Playwright headless, suchanyay@chememan.com, restarted local uvicorn to pick up the change): grid GL count 11→9 rows after the fix, 0 outliers vs the 134-row `GET /budget/gl-accounts` master, 0 `.reference-hint` elements, 0 console errors — PASS. Confirmed live the 2 dropped GLs (`5120500010`, `6410200250`) are genuinely absent from `dbo.gl_group`; company-wide SAP-actuals impact across all cost centers ≈ 1,730,730.37 THB now hidden (only ≈15,437 THB of it was in suchanyay's own Fill/See scope) — **flagged for jakkaritw to sanity-check the volume** before this is considered final. Subtotal/financial-correctness guard proven via unit test (`test_master_filter_subtotal_excludes_dropped_row_no_double_count`): unfiltered subtotal 1,500 → filtered 1,000, hidden row's amount never double-counted (structural — the merge function's returned row list IS what every downstream subtotal sums, so a dropped row can never leak into a total). Not committed (per instructions).

## Fabric SQL migration — Azure SQL retirement (ADR-0017)
- [x] `setup/sync_employees.py` → Fabric SQL DB via Service Principal (ActiveDirectoryServicePrincipal, ENTRA_CLIENT_ID/SECRET) — verified: SP auth OK, mas_employee_data = 343 rows, API 645 Active → 343 include (in sync)
- [x] `.github/workflows/sync_employees.yml` → drop Azure SQL firewall steps + DB_* env; pass FABRIC_SQL_* + ENTRA_* secrets
- [x] root `db/connection.py` (Azure SQL orphan) → archived to `bin/azure-sql-legacy/`
- [x] **USER: GitHub secrets** — already existed as `FABRIC_AAD_*` + `FABRIC_SQL_*`; yml repointed to them (no new secrets needed)
- [x] verify daily Action runs green — ✅ confirmed in prod 2026-06-14 (synced to Fabric SQL DB via SP)
- [ ] after green several days → delete Azure SQL server `cman-budget-mngt-web-sql` + GitHub secrets `DB_*` + `.env` `DB_*`

## Real build (next) — sequence grilled 2026-07-11/12 (see memory project_nextstep_design_decisions)
- [ ] **build sync `cc dept.xlsx` → Fabric** (single gating dependency: CC→ฝ่าย feeds approval unit AND CC→Filler feeds RLS; Filler = UNION across duplicate CC rows). Validation-on-ingest: (a) WARN per Filler email not found in the unfiltered employee source; (b) WARN if a ฝ่าย has non-uniform Filler sets across its CCs (currently 0 — invariant that keeps whole-ฝ่าย Submit safe per ADR-0008); skip blank-Filler CC rows individually (ADR-0019). **Target = `[fabric_sql_database].[dbo]` (cman-dw-ws, next to employee_master — ADR-0022); cadence = DAILY, same as employee_master; exploded grain (1 row per CC × filler_email). All 8 SharePoint masters land here on the same daily cadence.** (grill 2026-07-14)
- [x] **employee source for RLS + approver1 CONFIRMED** (real-data verified 2026-07-15 against live `dbo` in `fabric_sql_database`): `dbo.v_employee_budget_01` (497 rows, filtered) covers 100% of Fillers + 100% of Filler-manager rows (`A_fillers_missing_497=0`, `C_mgrs_missing_497=0`); approver1 email denormalized on-row via `manager_email`; empcode 101930 (thanakorny) has 0 Filler subordinates — the old "must not be dropped" GAP is moot. `v_employee_primary` (612) kept as fallback only (full-roster needs, e.g. trip traveler picker). Docs locked: `docs/specs/budget-transactional-data-model.md` (§1b + manager-resolution note + §4 email-match rule), `docs/BUILD_PLAN.md` A3, `docs/DATA_PIPELINE_PLAN.md` deliverable #5 + Prereqs item 2, `docs/adr/0019-cc-filler-map-replaces-orgcode-rls.md` (dated note).
- [x] **DDL `budget.*` transactional tables on Fabric SQL DB (DB1, budget_management_web)** — `db/ddl/budget_transactional_tables.sql` written 2026-07-14 (5 of the 7: pending_budget, pending_budget_detail, budget_trip, approval_status[PK department,fiscal_year], approval_log; board_budget + submission_deadline are read-only masters in the DW DB, excluded from DB1). NOT run — jakkaritw reviews + runs in Fabric SQL editor. Added CHECK constraints (template, country_group, side) beyond raw col list — flagged for review.
  - **REVERSED + consolidated 2026-07-14 (ADR-0023):** ONE DB `fabric_sql_database` (DW `cman-dw-ws`, a42ef9f3), two schemas — `budget.*` transactional (retargeted off the now-RETIRED DB1 `budget_management_web` 036a3270) + `dbo.*` synced masters/board_budget/submission_deadline in the SAME DB. DDL header/schema-block + spec + BUILD_PLAN + DATA_PIPELINE_PLAN reconciled; new ADR-0023, pointer lines on 0017/0022. Read path board+pending+cc_filler = local cross-schema JOIN, only SAP cross-store. **Config to-do: env FABRIC_SQL_SERVER/DATABASE must re-point to `fabric_sql_database`.** NOT run / NOT committed.
- [ ] **board_budget SharePoint-drop sync** (ADR-0021: `approved_budget_<year>.xlsx`, sheet `sheet1`, cols A–N, year from filename strict; trigger open: Sync-now button vs daily poll)
- [ ] scaffold frontend/ (React+Vite) + backend/ (FastAPI) — main budget app (mockup **0002.2** = source; 0002.1 superseded)
  - [x] **A2 increment 1 (backend foundation) done 2026-07-15**: `backend/app/{main,config,auth,db}.py` + `routers/{health,me}.py` — Entra Easy Auth header extraction w/ local `DEV_AUTH_EMAIL` override (fail-closed default `APP_ENV=production`), 2 lazy context-managed pyodbc connection factories (`get_fabric_conn`/`get_gold_conn`, ODBC 17, SP `cman-fabric-write`), `GET /health` (+`?deep=1`), `GET /me`. 25 unit tests green (pyodbc fully mocked, no live DB); live uvicorn smoke-test confirmed boot + 401/200 behavior. NOT built: RLS (A3), read path (A4), write (A5), approval (A6), frontend (A7+). `.env` NOT edited — `backend/.env.example` documents the required re-point to `fabric_sql_database` (ADR-0023) once the DATA team confirms the FQDN. Not committed (per instructions).
  - [x] **A3 (RLS scope resolution) done 2026-07-15**: `backend/app/rls.py` (`resolve_scope(email, conn, admin_view_enabled=False, settings=None) -> Scope{email,is_admin,role,fill_cost_centers,see_cost_centers}`) + `routers/scope.py` (`GET /scope`, wired in `main.py`) + `config.py` `ADMIN_EMAILS`→`admin_emails_set`. Fill = `cc_filler_map WHERE LOWER(filler_email)=LOWER(@me)`; See = Fill ∪ manager-add (`cc_filler_map JOIN v_employee_budget_01 ON email WHERE LOWER(manager_email)=LOWER(@me)`), one in-DB JOIN, deduped in Python as a backstop. Role = admin > filler > see_only > none. 16 new unit tests green (42 total incl. A2), pyodbc cursor fully mocked — no live DB. NOT built: read path (A4), write (A5), approval (A6). **Live-DB precondition still open**: env FABRIC_SQL_SERVER/DATABASE not yet re-pointed to `fabric_sql_database` (ADR-0023) — no integration test added, nothing to run against yet. Not committed (per instructions).
  - [x] **A3 gate hardening (3 non-blocking items) done 2026-07-15**: `rls.py` blank-email guard (short-circuits to `role="none"` empty scope, no SQL) + cursor wrapped `try/finally cursor.close()` (kept `conn.cursor.return_value` identity so existing mocks stay valid — real `with`-statement would've required rewriting all 12 mocks); `test_rls.py` +1 regression test locking `admin_view_enabled=True` cannot self-elevate a non-admin. 43/43 pass. Not committed (per instructions).
  - [x] **A4 (main-table read path) done 2026-07-15**: `backend/app/sap.py` (`fetch_sap_actuals` — SAP_ACTUALS_SQL copied verbatim from ADR-0020/BUILD_PLAN, excluded-CC list WITHOUT `10SC012000`, no sign flip/no doc_status filter, pivots `period_month`→m01..m12, raises `SapActualsFetchError` fail-loud on query error) + `backend/app/read_model.py` (`fetch_board_pending_rows` = local `dbo.board_budget` **FULL OUTER JOIN** `budget.pending_budget` on (cost_center,gl_account) — used FULL OUTER not LEFT to satisfy ADR-0010's persistence guarantee for pending-only manually-added rows and board-only Approved-import rows, not just SAP-led; `merge_budget_rows` = pure RLS/visibility merge, `get_budget_grid` = orchestrator) + `backend/app/routers/budget.py` (`GET /budget?year=<planning_year>&cost_center=&department=&admin_view_enabled=`, SAP/DB failure → 502, never silent-empty). Year mapping confirmed from spec §1c: `year` param = Pending planning year (Y+1); board_budget + SAP both use `year-1` (standing/current year Y); Approved-Y is reference-only, requested-vs-granted is Phase-2. Row model = 3 layers always present (zero-filled, never null) + `editable`; admin-wide bypass only when `is_admin AND admin_view_enabled` (non-admin cannot self-elevate, regression-tested). 35 new unit tests (9+20+6), all mocked — 78/78 total pass. NOT built: write (A5), approval (A6). GL-name/group snapshot for a pure SAP-led row (no board/pending yet) is null until A5's write path creates the row — flagged for A5/A8. `.env` NOT edited, not committed (per instructions).
  - [x] **A4 gate hardening (5 non-blocking items) done 2026-07-15**: `read_model.py` — SQL-side RLS pushdown: `fetch_board_pending_rows(..., cost_centers=None)` adds parameterized `AND cost_center IN (?,...)` to BOTH join-side subqueries (never string-interpolated); `None`=admin-wide bypass (only when `is_admin AND admin_view_enabled`, decided in `get_budget_grid`); `[]`=short-circuits to empty result with NO query executed (avoids invalid `IN ()`); Python-side filter in `merge_budget_rows` kept as belt-and-braces backstop (not removed) — both layers active. Also replaced the two join subqueries' `SELECT *` with explicit column lists. `routers/budget.py` — 502 handler no longer leaks `str(exc)`; now `logger.exception(...)` server-side + generic client detail `"SAP actuals unavailable, please try again later"` (matches `health.py` pattern). `main.py` docstring corrected (A4 read path IS built here now). 7 new unit tests (6 read_model + 1 router, incl. locking in that a raw `pyodbc.Error` also returns 502) — 85/85 total pass. SAP_ACTUALS_SQL untouched (verified 0 diff). `.env` NOT edited, not committed (per instructions).
  - [x] **A5 (budget write path) done 2026-07-15**: `backend/app/per_diem.py` (`derive_per_diem` — pure calc, no I/O; `days × rate(position,country_group) × FX(year)`, floor-split per month with the LAST selected month absorbing the rounding remainder so `sum(m01..m12)` == exact total at DECIMAL(18,2); raises `MissingPerDiemRateError`/`MissingFxRateError` — never a silent fallback) + `backend/app/special_gl.py` (GL-conditional `meta_json` validation for Entertainment 030/031-suffix + Lease & Rental suffix-driven dropdown/grey-out, spec §4a; the other 3 special groups are free-form per spec) + `backend/app/write_model.py` (`save_pending_rows`/`save_detail_lines`/`save_trip` — all batch-shaped, each item processed independently; row-grain optimistic lock via atomic `UPDATE...WHERE _updated_at=?` + rowcount check, brand-new-row PK collision → `pyodbc.IntegrityError` caught as conflict; fill-scope-or-admin gate via A3 `resolve_scope`; excluded-CC list kept in sync with `sap.py`; special-GL cells blocked from direct `/budget/rows` edit; trip `side` enforced against the 8 travel GLs so COST/SGA never cross; parent `pending_budget` cell recomputed as SUM(detail) after every detail/trip write; `budget.approval_status` never referenced, `pending_budget.status` only set 'DRAFT' at creation) + `backend/app/routers/budget_write.py` (`PUT /budget/rows`, `PUT /budget/detail`, `POST|PUT /budget/trip`; `ERROR_HTTP_STATUS` maps business error codes → 400/403/409, missing-FX/rate propagate to a 500). 69 new unit tests (14+17+29+9), all mocked — 154/154 total pass. **Flagged (not invented):** `gl_name` (individual GL display name) has no confirmed reference home yet (DATA_PIPELINE_PLAN item 4 GAP) — left NULL every snapshot; `dbo.gl_group` assumed cols `gl_code`/`group_name` per that same doc, unverified against the live synced table. A6 (approval engine) still owns `budget.approval_status` entirely. `.env` NOT edited, not committed (per instructions).
  - **A5 gate hardening (4 MUST-FIX + 3 cheap items) done 2026-07-15**: gate returned REQUEST-CHANGES, all fixed TDD (RED confirmed before each fix): (1) `_save_one_detail_line` commit moved to AFTER `_recompute_parent_cell` — old order let `db.py`'s non-committing `close()` silently roll back every special-GL parent-cell recompute (real data loss, hit all 5 non-trip special groups); (2) `_recompute_parent_cell`'s parent-cell INSERT now catches `pyodbc.IntegrityError` (concurrent first-ever writes to the same cell) and retries the UPDATE or raises `RowConflictError` → 409 instead of an uncaught 502; (3) the 3 non-per-diem Travelling GLs (transport/accommodation/other) now require `trip_id` — `None` used to bypass the COST/SGA side-check entirely and save an orphan line; (4) trip `side` update (COST↔SGA) now captures the OLD side via `_lookup_trip` before overwrite, deletes the old GL's per-diem line, and recomputes the OLD GL's parent cell too (old bug: ghost amount survived forever under the stale GL, only the new GL was recomputed). Cheap fixes: `TripInput.country_group`/`days` now Pydantic `Field`-validated (422 instead of a generic 500 from a `derive_per_diem` `ValueError`); `_run_per_item` calls `conn.rollback()` on a caught per-item failure so no partial transaction leaks across batch items; admin Fill-scope bypass now also checks CC existence (`_ensure_cost_center_exists` against `dbo.cc_filler_map`) via new `UnknownCostCenterError`/`unknown_cost_center` (400) — non-admins already covered since their scope is derived from the same master. 8 new tests + 3 existing trip/admin tests updated for the new legitimate DB calls (not weakened) — 162/162 total pass. Logged, not fixed (per gate instruction): money stays `float` at the API boundary (Decimal only inside `per_diem.py`) — flagged as a follow-up, not contained enough to do inline; `ERROR_HTTP_STATUS` dead entries (`missing_per_diem_rate`/`missing_fx_rate` never read, harmless); Training & Seminar's `Method` dropdown un-validated (non-blocking per gate). `.env` NOT edited, NOT committed (per instructions).
  - **A5 re-gate BLOCKER fixed 2026-07-15**: `_save_one_detail_line` was missing the `_ensure_not_excluded(cost_center)` call present in the other two write paths — an admin (Fill-scope bypassed per ADR-0012) could save a detail line for an excluded CC (CMRY01 etc.), and `_recompute_parent_cell`'s auto-create INSERT would silently create a `pending_budget` row for it, breaking the SAP-actuals control-number reconcile (never-cut). Fixed by calling `_ensure_not_excluded` before `_ensure_write_scope`, matching `_save_one_pending_row`/`_save_one_trip` ordering (TDD: RED confirmed — pre-fix, non-admin got wrong error code `forbidden` and admin's call reached the DB with `not_special_gl`; both now correctly `excluded_cost_center`, no DB call). Also: (a) added a companion test proving the PK-collision retry path can SUCCEED, not just fail-to-409 (previous test's static `rowcount=0` mock couldn't distinguish the two); (b) non-Travelling detail lines now reject a non-null `trip_id` (`InvalidRequestError`, defense-in-depth — previously zero cross-validation); (c) `write_model.py` module docstring documents the 422 (Pydantic shape) vs 400/403/409 (named business rule) split, no behavior change. 3 new tests — 165/165 total pass. `.env` NOT edited, NOT deployed, NOT committed (per instructions).
  - [x] **A5 FIRST live-DB integration test done 2026-07-15** (`backend/tests/test_integration_live.py`, `@pytest.mark.integration`, skipped by default): proved the A5 gate fix on the REAL Fabric SQL DB — save Entertainment 1,000 → API `ok=True` → fresh connection reads back `m01=1000`/`total_year=1000` in both `budget.pending_budget` (parent) and `budget.pending_budget_detail`; 2nd line (500) → parent sums to 1,500; A3 `resolve_scope` proven non-empty Fill + See⊇Fill against real `dbo.cc_filler_map`/`v_employee_budget_01`. Sentinel `fiscal_year=2099`, cleanup verified 0 rows left. **2 real production bugs the live DB exposed (mocks had hidden both):** (1) `write_model._lookup_gl_group` selected a non-existent `group_name` column — real `dbo.gl_group` cols are `gl_code`/`gl_group`/`gl_name` (confirmed via `INFORMATION_SCHEMA.COLUMNS`); fixed to select `gl_group`. (2) BIGGER: `write_model.py`'s two `pending_budget` INSERTs (and `read_model.py`'s `_PENDING_META_COLUMNS`/`PendingLayer`) assumed a `status` column that **does not exist** on the live table — the DDL (`db/ddl/budget_transactional_tables.sql`) explicitly documents "NO status column (status lives on approval_status, spec §5 Q4)" and the live table matches that design; the app code was wrong, not the schema. Every real INSERT into `pending_budget` would have raised `Invalid column name 'status'` (42S22) on first live use — this affected BOTH A5 write and A4 read paths. Fixed: removed `status` from both INSERTs + `_PENDING_META_COLUMNS` + `PendingLayer` (2 mocked tests in `test_read_model.py` updated to match). 165/165 mocked + 3/3 live all green after fix. `.env` NOT edited, NOT committed (per instructions).
  - [x] **GL-name GAP resolved 2026-07-15 (follow-up to the live-DB run above):** `dbo.gl_group` already carries `gl_code`→`gl_name` alongside `gl_code`→`gl_group` — the `docs/DATA_PIPELINE_PLAN.md` item 4 GAP (seed `dbo.gl_account_ref` vs a new SharePoint master file) is moot, no data work needed. `write_model._lookup_gl_group` now selects both columns in one query and `_derive_dim_snapshot` populates real `gl_name` (was always `None` before). Real value observed live: gl_code `5211900030` (Entertainment) → gl_name `ค่าเลี้ยงรับรองภายนอก`. Extended `test_write_model.py` (27 mocked GL-lookup fixtures now 2-column + 1 explicit `gl_name` assertion) and `test_integration_live.py` (asserts persisted `pending_budget.gl_name` is non-null and matches `dbo.gl_group.gl_name`). 165/165 mocked + 3/3 live green. **Read path (`read_model.py`) intentionally NOT changed** — a pure-SAP-led row (no board/pending yet) has no `gl_name`/`gl_group` field on its `SapLayer` at all; resolving that would mean adding a new `dbo.gl_group` I/O fetch and threading it into the currently I/O-free `merge_budget_rows`, which is a structural change, not a contained one — flagged as a follow-up (candidate: A8/dashboard pass). `.env` NOT edited, NOT deployed, NOT committed (per instructions).
  - [x] **A4 read path + SAP read-through FIRST live-DB run done 2026-07-16** (`backend/tests/test_integration_live.py`, +5 tests, 8/8 `@pytest.mark.integration` green): introspected live `INFORMATION_SCHEMA.COLUMNS` for `dbo.board_budget`/`budget.pending_budget`/`gold.fact_gl_trans` — A4's column assumptions already matched (the shared `status`-column bug was already fixed by the A5 live pass); `gold.fact_gl_trans` confirmed live at 23.7M rows (`gold_bi.fact_gl_trans` = 0 rows, decoy). **1 real bug found+fixed:** `app.sap.fetch_sap_actuals` called `conn.cursor()` OUTSIDE the try/except, so a connection-level failure (closed conn) raised raw `pyodbc.Error` instead of `SapActualsFetchError` — moved inside try, +1 mocked regression test (`test_sap.py`, 166/166 mocked green). **SAP parity proven live (never-cut, ADR-0020):** fiscal_year=2026, 1722 (cc,gl) keys, code SUM = independent hand-written SUM = 309,049,478.15 THB exactly; pivot m01..m12 matches per-key total. Board year discovered = 2026 (364 rows); real filler discovered `kwanreuthaih@chememan.com` (fill=6 CC, see=11 CC) — RLS proven on real data: 0 out-of-scope rows, 14 See-only rows all `editable=False`, 15 Fill rows all `editable=True`, SAP-led rows with blank pending confirmed. 3-layer sentinel round-trip (fiscal_year=2099) confirmed + cleanup verified 0 rows left. `.env` NOT edited, NOT committed (per instructions).
  - [x] **`app.sap` cost_center NULL-exclusion made explicit (2026-07-16, D2 follow-up)**: added `AND cost_center IS NOT NULL` to `SAP_ACTUALS_SQL` next to the existing `cost_center NOT IN (...)` — behavior-identical (NULL was already excluded via `NULL NOT IN (...)` = UNKNOWN), only makes the exclusion deliberate/self-documenting per ADR-0020's ⚠️ warning (do NOT NULL-safe this one the way `assignment_number` was fixed — would pull ~1.1B THB of no-cost-center postings into every cell). Docstring + inline comment added; no other filter changed (verified 0 diff on all other predicates). 2 new mocked tests (`test_sap.py`, 196/196 total mocked pass) + 1 new live test (`test_integration_live.py`, proves OLD-form vs NEW-form SQL return byte-identical keys/totals against the real warehouse, 16/16 integration pass). Live FY2026 observed post-D2: 1,722 keys, 312,921,505.04 THB (differs from the stale pre-D2-assignment-fix constant 309,049,478.15 as expected — D2 legitimately changed the total by re-including NULL-assignment rows). `.env` NOT edited, NOT committed (per instructions).
  - [x] **A6 (approval engine) done 2026-07-16**: `backend/app/approval.py` (state machine DRAFT/PENDING_APPROVER1-3/APPROVED/REJECTED; 3 FIXED chain positions — position1=dynamic `approver1_empcode`, position2=Nipaporn/`101032`, position3=Waraporn/`100427` constants, no stored empcode column needed for 2/3; self-skip+dedup+invalid-approver1-fallback unified into ONE mechanism via `_active_positions` — dedup naturally collapses an invalid/self approver1 onto the Nipaporn slot; `submit_department`/`approve_department`/`reject_department`) + `backend/app/routers/approval.py` (`POST /approval/submit|approve|reject`, `GET /approval/status`), wired in `main.py`. Submit branches: filler-of-department (incl. Nipaporn/Waraporn's own dual-role depts) → normal chain; admin-not-filler + `template='ADMIN'` rows → `ADMIN_SUBMIT`; admin-not-filler + orphan (0 rows in `cc_filler_map`) or post-deadline → `ADMIN_OVERRIDE`; admin-not-filler + normal in-cycle dept → 403. Reject→REJECTED(editable)→resubmit re-freezes the WHOLE chain (clears actioned_at/reject_reason, never resumes). Approve/reject step-gated by frozen empcode only — **no admin bypass** (ADR-0012 only documents admin EDIT/SUBMIT overrides, not an approve/reject bypass; the ADR-0009 "reassign a stuck approver" admin view is a separate not-yet-built feature, flagged not invented). Concurrency: conditional `UPDATE ... WHERE status=?` (optimistic, atomic) — race → `ConcurrentApprovalError` 409, proven live with 2 threads. **Live schema matched spec exactly** for `budget.approval_status`/`approval_log`/`dbo.v_employee_budget_01`/`dbo.cc_filler_map` (no mismatches found this time); `dbo.submission_deadline` has MORE live columns than the model's DBML (`closing_date/month/year`, `reminder_day`) but also already stores precomputed `deadline_date`/`reminder_date` — no bug, just simpler than expected. 34 new mocked tests (`test_approval.py`) + 12 new router tests (`test_approval_router.py`) — 242/242 mocked pass. 10 new live tests in `test_integration_live.py` using REAL Nipaporn/Waraporn/discovered-filler chains at sentinel `fiscal_year=2099` (self-skip, dedup, full 3-step chain, step-gating 403, reject+resubmit restart, admin orphan + Template-2 direct-approve, concurrent double-approve race) — 26/26 live pass, 0 residual rows verified across all 5 `budget.*` tables @2099 after the run. **Flagged, not invented:** (a) "orphan department" is detected as "0 rows in `dbo.cc_filler_map` for this department name" — ADR-0019 says blank-Filler CC rows are skipped individually at sync (no admin auto-fallback for CC ownership), so a true orphan ฝ่าย's CCs never land in `cc_filler_map` at all; there is currently no live table enumerating a department's CCs independent of Filler presence, so this proxy is the best available signal and is operationally safe (only reachable by an admin) but cannot be told apart from a typo'd department name — noted for A7's admin UI to constrain department names to a real picklist; (b) admin approve/reject bypass NOT built (see above); (c) A5's write path still has no year-lock/deadline check at all — A6's submit enforces it for normal users, but editing via `PUT /budget/rows` etc. does not yet reject a past-deadline write, flagged as a gap for whoever owns A5 hardening. `.env` NOT edited, NOT deployed, NOT committed (per instructions).
  - [x] **A6 gate hardening (2 MUST-FIX + 4 cheap items) done 2026-07-16**: gate returned REQUEST-CHANGES, all fixed TDD. **B1** `GET /approval/status` had NO authorization (any authenticated caller could read any department's submitter email/empcode/rejecter/reject_reason) — added `authorize_status_view(conn, department, scope)`: admin bypasses, else caller must See ≥1 cost center of the department via `dbo.cc_filler_map` (same mapping `submit_department` uses), unauthorized-real-department and nonexistent-department raise the SAME error (403) so the endpoint can't enumerate department names. **B2** `_admin_direct_approve`'s ADMIN_SUBMIT (Template-2 door) and orphan-department branches silently overwrote a mid-chain (PENDING_APPROVER1/2/3) or APPROVED record, skipping the real approvers — added fail-closed guard `_ensure_admin_overwrite_allowed` (409 `MidChainAdminOverwriteError` unless existing status is REJECTED or absent); the post-deadline branch is explicitly exempt (ADR-0012 override-everything unchanged). **Flagged for jakkaritw**: fail-closed is the shipped default — he may want admin to override mid-chain always; guard is isolated so flipping later is small. Cheap fixes: **S1** `_is_post_deadline` now anchors "today" to `Asia/Bangkok` via `zoneinfo` (was server-local `date.today()`), `_now()` UTC timestamps unchanged, inclusive/exclusive semantics pinned by test; **S2** first-submit INSERT now catches `pyodbc.IntegrityError` (concurrent duplicate PK) → same 409 `ConcurrentApprovalError` instead of a raw 502; **S3** resubmit UPDATE now conditioned on `WHERE status = ?` (REJECTED) with a rowcount-0 → 409 guard, matching approve/reject's existing race protection; **S4** split the single `ACTION_ADMIN_OVERRIDE` log code into `ADMIN_OVERRIDE_ORPHAN` / `ADMIN_OVERRIDE_DEADLINE` (live-verified `budget.approval_log.action` is NVARCHAR(40), both fit). 23 new mocked tests (`test_approval.py` +21, `test_approval_router.py` +2) — 265/265 mocked pass. 1 new live test (B1: real filler queries a real department outside their See scope → 403) + 1 existing live test's assertion updated for the S4 rename — 27/27 live pass, 0 residual sentinel rows (`fiscal_year=2099` across all 5 `budget.*` tables + `department=ZZ_TEST_DEPT_A6_NEVER_REAL`) verified after the run. Not committed (per instructions).
  - [x] **A5 final gap closed 2026-07-16 — deadline lock on the write path** (flagged by the A6 gate, item c above): `backend/app/deadline.py` (NEW — `is_post_deadline`/`bangkok_today`/`PastDeadlineError` extracted verbatim out of `approval.py`, both modules now import the same implementation so Submit and Edit can never disagree) + `backend/app/write_model.py` (`_ensure_not_post_deadline` helper — admin exempt, else `PastDeadlineError` → `past_deadline`/403, registered in `ERROR_HTTP_STATUS`/`_ERROR_CODE_BY_EXCEPTION`; called as the LAST check before the first DB write in `_save_one_pending_row`/`_save_one_detail_line`/`_save_one_trip`, so create+update+the trip side-flip path are all covered by one call site each). Missing-deadline-row policy matched A6 exactly (fail-OPEN, never silently lock an unconfigured year); Bangkok-anchored + deadline-day-inclusive semantics also matched exactly (same shared function). 7 new mocked tests (`test_write_model.py`) + 18 existing tests updated for the new legitimate DB call (mock sequencing only, no weakened assertions) — 291/291 mocked pass (was 265). 2 new live tests (`test_integration_live.py`, sentinel `fiscal_year=2097`, distinct from A6's 2099 — the ONLY test allowed to write `dbo.submission_deadline`): non-admin rejected + no row written, admin succeeds — 31/31 live pass, 0 residual rows verified at both 2097 AND 2099 across all `budget.*` tables + `dbo.submission_deadline`. `.env` NOT edited, NOT committed (per instructions).
  - [x] **A7 (frontend scaffold) done 2026-07-16**: `frontend/` Vite+React+TS project created (`npm create vite@latest frontend -- --template react-ts`, no global installs). `src/api/{client,types}.ts` (typed `apiFetch` — 401 → `/.auth/login/aad?post_login_redirect_uri=` per ADR-0004, 403/5xx/network mapped to `ApiError`, centralized so every future endpoint gets the same 401 handling for free) + `src/auth/{useAuth,useScope}.ts` (`GET /me`, `GET /scope` → `{role,is_admin,fill_cost_centers,see_cost_centers}` per ADR-0019) + `src/filters/deepLink.ts` (parses `?dept=&year=` per ADR-0016, convenience-only, year validated 4-digit + ±5yr sane window, dept non-empty-after-trim, missing/invalid params ignored individually never a crash) + `src/styles/{tokens,global}.css` (colors/fonts/radius/dark-light ported verbatim from canonical mockup `0002.3budget-export.html` `:root`/`[data-theme=dark]`) + `src/App.tsx` shell (nav w/ theme toggle, user-bar showing email/role/Fill·See CC counts, deep-link filter chip, grid placeholder text for A8). **Decisions (no ADR conflict, flagging for visibility):** no router (single page per ADR-0016, query params read directly), no state library (2 hooks covering 2 GET calls), vite dev proxy forwards route namespaces directly (`/health /me /scope /budget /approval`) with NO `/api` prefix since `backend/app/main.py` mounts none. 24 Vitest+RTL tests green (deepLink parser incl. URL-encoded Thai + sane-year-window; useAuth 401→redirect via mocked `window.location`; useScope success+failure; api client 401/403/5xx/network mapping; App shell renders with/without deep-link, resolved user+scope). `npm run build` (tsc -b + vite build) green. Extra real-browser Playwright headless sanity check (7/7 PASS, 0 console errors, no screenshot per no-screenshot-for-logic policy) against the actual `npm run dev` server with `/me`+`/scope` route-mocked. `frontend/.gitignore` (from create-vite scaffold) already ignores `node_modules`/`dist`. Not committed (per instructions). NOT built: A8 grid/A9 subforms/A10 approve-inline+admin-toggle — placeholder text only.
  - [x] **A7 gate mechanical fixes done 2026-07-16**: `tsconfig.app.json`/`tsconfig.node.json` add `"strict": true` (gate-verified zero new tsc errors) + `deepLink.test.ts` 4 new boundary tests (currentYear±5 accepted, currentYear±6 rejected, computed dynamically) + `App.test.tsx` hardcoded `year=2026` replaced with dynamic `new Date().getFullYear()` in both the deep-link test and its assertion so the test doesn't rot when 2026 ends. 28/28 tests pass (24+4), `npm run build` green. Not committed (per instructions).
  - [x] **A8 (main budget grid) done 2026-07-16**: `frontend/src/grid/{model,MonthCell,GridTable,AddTransactionForm,BudgetGrid,YearPicker,testUtils}.{ts,tsx}` + `frontend/src/picker/{model,DeptPicker}.{ts,tsx}` — 3-layer grid (SAP/Approved RO, Pending editable), COST 5xxx/SG&A 6xxx structurally separate sections+subtotals per gl_group (never-cut, never combined — enforced by `sectionTotals`/`groupAndSortBySide` never exposing a cross-side sum), ฝ่าย picker (สายงาน›ฝ่าย(count)›CC(count), mirrors mockup `.faip`), year picker, inline month-cell edit → one `PUT /budget/rows` per row on blur (payload = full 12 months + `expected_updated_at` lock token), 409 → refetch + Thai "ถูกแก้ไขโดยผู้อื่น" message, special-GL cells always blocked (disabled+tooltip "แก้ไขผ่านฟอร์มย่อย") regardless of `row.editable`, "+ เพิ่ม transaction" (Fill-scope CC × non-special GL, client-validated then `PUT .../rows` create path), loud error state on grid-fetch failure (never silent-empty). **Backend additions** (new module, no conflict w/ the parallel A5-deadline work): `backend/app/reference_data.py` + `backend/app/routers/reference.py` — `GET /budget/gl-accounts` (full `dbo.gl_group`, `is_special` flag) + `GET /scope/departments` (`dbo.cc_filler_map` dims, scoped like `GET /budget`); both live-tested (2 new tests in `test_integration_live.py`, 29/29 live pass), column names reused from A4/A5's already-live-proven queries (no fresh introspection needed). **1 real backend GAP fixed** (flagged, not silently invented): `read_model.PendingLayer` never exposed `_updated_at` — impossible for the frontend to edit an EXISTING pending row (only ever create), since `write_model`'s optimistic lock needs that token back. Added `pending.updated_at` (threaded through the board+pending JOIN SQL, mirroring the existing `total_year` column pattern) — 3 new + 34 existing `test_read_model.py` tests green, live-proven (29/29 integration). **1 real frontend bug caught by TDD**: `MonthCell`'s local `draft` state never resynced when the `value` prop changed externally (same-key component reuse across re-renders) — broke the 409-conflict-revert flow silently; fixed with a `useEffect([value])` resync, regression-tested. **Decisions flagged**: (a) GL metadata (gl_group/gl_name/is_special) resolved for EVERY row via the new `/budget/gl-accounts` reference list, not per-layer fields — sidesteps the known gap where a pure-SAP-led row has no gl_group of its own; (b) admin toggle (ADR-0014) NOT built (deferred to A10 per brief) — pure admins (`is_admin` + empty Fill/See, e.g. jakkaritw) get `admin_view_enabled` auto-true (no UI toggle, matches ADR-0014's "no base hat, always admin"); dual-role overlay admins stay scoped like a normal user in A8. 109 frontend tests green (was 28+81 new), `npm run build` green, backend 291/291 mocked + 29/29 live green. Playwright headless verify 13/13 PASS (load/3-layers, edit+save payload, 409+revert, special-GL block, add-transaction, deep-link prefill), 0 console errors, light+dark screenshots saved to scratch (`a8_grid_{light,dark}.png`) — user to review before further sign-off. NOT built: A9 subforms/Trip Manager, A10 approve-inline/admin-toggle-UI/attachments. `.env` NOT edited, NOT committed (per instructions).
  - [x] **A9 (special-GL subforms + Trip Manager) done 2026-07-16**: `frontend/src/subform/{model,glDropdownConstants,DetailSubform,TripManager}.{ts,tsx}` — `DetailSubform.tsx` (5 non-travel special groups: Entertainment/Lease & Rental/Professional & Legal Fee/Public Relation & Donation/Training & Seminar; per-row Save via `PUT /budget/detail`, one line = one call, matching the backend's per-item contract) + `TripManager.tsx` (trip-centric entry: header form → `POST/PUT /budget/trip`, per-diem NEVER computed client-side — shows "ระบบจะคำนวณเบี้ยเลี้ยงให้หลังกดบันทึก" until the server responds, then the server's own number, labeled "จากเซิร์ฟเวอร์"; 3 manual expense types (transport/accommodation/other) save via the SAME `/budget/detail` endpoint with `trip_id` attached, month cells locked to the trip's `travel_months` per ADR-0005). GL-conditional dropdowns (Entertainment 030/031-suffix, Lease & Rental suffix-driven grey-out) mirror `backend/app/special_gl.py` exactly via ONE shared fixture `docs/reference/special-gl-dropdown-fixture.json` (tracked in git — `docs/13Template Special/` is gitignored, large Excel dumps) + a parity test on EACH side (`test_special_gl_fixture_parity.py` backend, `glDropdownConstants.test.ts` frontend) — a drift on either side fails immediately; also covers `write_model.TRAVEL_GL_BY_TYPE_SIDE` (COST/SGA GL map, never-cut). After every line/trip save, `onSaved()` triggers the parent grid's full refetch so server-recomputed parent-cell sums show immediately (both sides, in case of a trip side flip). **Backend gap-fill (flagged, read-only, additive — did NOT touch `write_model.py`/`budget_write.py`/`approval.py`):** A5 shipped save-only endpoints with no matching GET, so a subform could create a line but never see/edit an existing one, and the `expected_updated_at` lock token had no source — added `backend/app/subform_read.py` (`fetch_detail_lines`, `fetch_trips` — per-diem recompute-on-read per trip, ADR-0015/0011, reusing (importing, not modifying) `write_model`'s already-bug-fixed `_lookup_per_diem_rate`/`_lookup_fx`; one trip's missing-rate/FX becomes `per_diem_error` on that trip only, never a 500 for the whole list) + `backend/app/routers/subform.py` (`GET /budget/detail`, `GET /budget/trip`, See-scope gated). 373→377 backend mocked tests green (66 new: subform_read/router/fixture-parity), 109→174 frontend tests green (65 new), `npm run build` green. **Real backend gap NOT invented, flagged for the write-path owner:** there is NO delete endpoint anywhere (`write_model.py` has no delete function, `budget_write.py` has no DELETE route) for either detail lines or trips, even though the task spec calls for delete — UI ships add+edit only, with a visibly disabled "ลบ" button + Thai tooltip explaining it is not yet supported (chose honesty over a fake soft-delete-via-zero workaround, which would silently accumulate orphan rows). Playwright headless verify (mocked API): 2/2 scenarios PASS (Entertainment subform add-line+save+parent-refetch; Trip create+per-diem-shown+parent-refetch), 0 console errors, light+dark screenshots saved to scratch (`a9_subform_light.png`, `a9_trip_manager_dark.png`) — user to review. `tsconfig.app.json` gained `resolveJsonModule: true` (needed for the shared fixture-parity test to import the JSON directly, avoiding a broader `"node"` types addition). NOT built: approve/reject UI, badges, admin toggle (A10, per brief). `.env` NOT edited, not committed (per instructions).
  - [x] **A9 DELETE gap closed 2026-07-16** (the "NO delete endpoint anywhere" flag above): `backend/app/write_model.py` — `delete_detail_line(conn, detail_id, expected_updated_at, user_email, scope)` + `delete_trip(conn, trip_id, expected_updated_at, user_email, scope)`, both resolving the row's ACTUAL cost_center/gl/fiscal_year(/side) from the DB by id ONLY — neither function's signature accepts cost_center/gl_account/fiscal_year at all, so unlike save's D3/D4 IDOR fix there is no payload field left to spoof in the first place. Same gates as every other write (Fill-scope-or-admin, `_ensure_not_post_deadline` on the row's real fiscal_year, optimistic lock via `_updated_at`), `_recompute_parent_cell` BEFORE commit (same commit-order rule). Deleting the LAST detail line is allowed: parent row is KEPT and zeroed (`SUM(detail)=0`), never removed. `delete_trip` removes the trip header + ALL its lines (per-diem + 3 manual types, one `WHERE trip_id=?` DELETE) then recomputes all 4 of that side's travel-GL parent cells unconditionally (simple + always safe). Reuses the existing `_run_per_item`/`ERROR_HTTP_STATUS`/`_ERROR_CODE_BY_EXCEPTION` infra — no new error codes needed. `backend/app/routers/budget_write.py` — `DELETE /budget/detail`, `DELETE /budget/trip` (query params: id + `expected_updated_at`). 20 new backend mocked tests (12 write_model + 8 router) — 402/402 mocked pass. 3 new live tests (`test_integration_live.py`, sentinel `fiscal_year=2095` — 2096/2097/2099 already taken — own cleanup helper) — 34/34 live pass, 0 residual rows verified at 2095 AND 2096/2097/2099 untouched. **Frontend**: `frontend/src/api/subform.ts` +`deleteDetailLine`/`deleteTrip`; `DetailSubform.tsx`/`TripManager.tsx` — the disabled "ลบ" buttons enabled (confirm dialog Thai text, unsaved/never-persisted rows removed locally with no API call/no confirm, 409 → refetch + Thai conflict banner, success → `onSaved()` triggers the existing grid refetch path unchanged). 174→184 frontend tests green (10 new: 3 api/subform.test.ts pairs + delete-flow tests replacing both disabled-button tests), `npm run build` green. Playwright headless verify (real Chromium, mocked routes, no live server): 2/2 scenarios PASS (DetailSubform delete removes the row + triggers grid refetch; TripManager delete removes the card + triggers grid refetch), 0 console errors — logic/workflow change, no screenshot per the no-screenshot-for-logic policy. `.env` NOT edited, not committed (per instructions).
  - [x] **A11+A12 (scheduled jobs + emails, "automations") done 2026-07-16**: `backend/jobs/{common,auto_submit,auto_escalate,send_reminders}.py` (`python -m jobs.<name> --fiscal-year <Y> [--execute]`, no auto-detected "current year" — always explicit, no existing helper derives one) + `backend/app/notifications.py` (Graph sendMail, `send_signoff_email.py`'s proven SP pattern via `httpx` not `requests`, ONE transport seam `send_mail` — `dry_run=True` default makes ZERO HTTP calls) + `.github/workflows/budget-automations.yml` (workflow_dispatch-only, cron commented out + go-live header note, job env `DRY_RUN='true'`). **auto_submit** (ADR-0006): targets TRUE DRAFT departments only (`pending_budget` rows for the year, NO `approval_status` row at all) — REJECTED depts deliberately excluded, left to admin's post-deadline override (ADR-0012) so exactly one automation owns that case; approver1 = last editor's (`pending_budget._user`) manager via the SAME `resolve_chain` a human submit uses; idempotent for free (2nd run's discovery query excludes anything with a row). **auto_escalate** (ADR-0006, 30-day SLA): advances ONE step via a new `_current_step_started_at` derivation (no dedicated per-step timestamp column exists — flagged, no schema change — derived as the latest earlier-position `actioned_at`, falling back to `submitted_at` when a position was self-skip/dedup-skipped); NEVER escalates an already-final active position (no silent jump to APPROVED); idempotent per window for free (escalating stamps `actioned_at=now`, so the next run's derived age is 0). **Turn-notify/reject-notify wired at the ROUTER** (`routers/approval.py`), not inside `app/approval.py`'s pure functions — `test_approval.py`'s shared-mock-cursor + finite `side_effect` convention would have broken ~40 existing tests if an unconditional DB lookup were added inside the state-machine functions themselves; the router already has everything notify needs off the returned `ApprovalStatusState` (additive `notification_warning` field, non-fatal). **Reminder job** (automation D): broader than auto_submit — includes REJECTED depts too (they still need to act before the deadline), grouped one email per Filler via `dbo.cc_filler_map`. **Flagged then RESOLVED (jakkaritw 2026-07-16: build it):** the final-APPROVED confirmation email to the submitter (from `docs/reference/approval-workflow.md`'s trigger table) was initially excluded, then built same-day as the 4th notification — `notify_approved` in `notifications.py` (frozen `submitter_email`, same pattern as reject-notify), router branch `approve → status APPROVED → notify_approved` (mid-chain approve still notify_turn), +3 notification tests +3 router tests; admin-direct-approve branches (ADMIN_SUBMIT/ADMIN_OVERRIDE_*) never trigger any email (no approver to notify). 46 new backend unit tests (13 notifications + 12 approval.py auto_submit/auto_escalate/helpers + 7 config/router + jobs: 5 common + 6 auto_submit + 6 auto_escalate + 7 send_reminders) — 350/350 mocked pass (was 291). 2 new live tests (`test_integration_live_jobs.py`, sentinel `fiscal_year=2096`, its own file — a parallel A8 agent owns `test_integration_live.py`): auto_submit dry-run→execute→idempotent-2nd-run, auto_escalate advances a real 31-day-stale row — both green against the real Fabric SQL DB, 0 residual rows verified across all `budget.*` (5 tables) + `dbo.submission_deadline` at 2096. NO real email sent (notifications_dry_run=True everywhere in tests/jobs by default), NO workflow cron active. `.env` NOT edited, not committed (per instructions).
  - [x] **A10 (approval UI + admin toggle + scope-role UX + attachments) done 2026-07-16 — last frontend piece of Phase-1**: **Backend additive-only** (no change to `write_model.py`/`budget_write.py`, `approval.py`'s state machine untouched): `app.approval.list_departments_pending_my_approval` + `GET /approval/pending-for-me?fiscal_year=` (รออนุมัติ badge source — reuses `fetch_pending_rows`+`_to_state.can_act`, no new SQL shape) + `backend/app/attachments.py` (NEW, SharePoint via Graph, no DB table per spec R1 — `list_attachments`/`upload_attachment`/`get_download_url`, `sanitize_department_folder` per spec's exact SharePoint-illegal-char rule) + `backend/app/routers/attachments.py` (`GET /attachments`, `POST /attachments/upload`, `GET /attachments/download-url`; upload=Fill-scope-or-admin, list/download=See-scope-or-admin, reusing the `dbo.cc_filler_map` department→CC check). **Attachments target was NOT a guess**: `docs/specs/budget-transactional-data-model.md` §4b (R1, "Root path RESOLVED 2026-07-13") already names site `CMANDWPRD` / library `Budgeting and Management` / folder `เอกสาร ฝ่าย/<ฝ่าย>/<year>/`, all 114×2 folders pre-created — shipped as `config.py` `attachments_*` settings defaulting to those confirmed values (still overridable via env, never hardcoded); a missing/blank override still fails loud (`AttachmentsNotConfiguredError`→501), a missing folder is `FolderNotFoundError`→502 (never a silent empty list). `requirements.txt` +`python-multipart` (FastAPI Form/File dependency, was missing). **Frontend**: `frontend/src/approval/{model,ApprovalActionBar}.{ts,tsx}` (status chip DRAFT/รออนุมัติขั้น1-3 with Nipaporn/Waraporn named at positions 2/3 — position 1 has no name in the API response so shows a role label instead of a raw empcode — /APPROVED/REJECTED+reason; inline Submit — Thai `window.confirm` summary, same pattern as A9's delete-confirm — Approve/Reject gated purely on the server's own `can_act`, no client-side approver-identity logic; reject reason via a real textarea panel, not `prompt()`; 409 → refetch + Thai message) + `frontend/src/admin/{useAdminViewToggle,AdminModeToggle}.{ts,tsx}` (sessionStorage-persisted, ADR-0014 default OFF; shown ONLY for dual-role admins — is_admin AND non-empty Fill/See; pure admins stay always-on/no-toggle per A8, unchanged; toggling resets the locked ฝ่าย) + `frontend/src/attachments/AttachmentsModal.tsx` (list/upload/download-url, upload hidden when `!canUpload`) + `DeptPicker.tsx` รออนุมัติ badge (trigger + list, from the new pending-for-me endpoint) + `BudgetGrid.tsx` wiring (no-scope role='none' → friendly empty state "คุณไม่มีสิทธิ์กรอกงบประมาณ — ดูข้อมูลได้ที่ Dashboard", BEFORE any budget/departments fetch; `see_only` keeps the full page per the brief). **Flagged, not invented (pre-existing A5/A6 gap, NOT fixed here — out of A10's file scope):** `write_model.py` has NO check against `budget.approval_status` at all — a submitter can still edit a Pending row while it is PENDING_APPROVER1/2/3 (should be locked, ADR-0006) or APPROVED (should require an explicit re-open confirm, ADR-0006/0013); A10 only shows an informational lock note in the action bar (mirrors the mockup's own note-only behavior) — recommend a fast follow adding an approval-status check to `write_model.py`'s write entry points, mirroring the existing deadline-lock pattern. 42 new backend mocked tests (4 approval + 3 router + 2 config + 19 attachments + 14 attachments_router) — 444/444 mocked pass (was 402); 2 new live tests (`test_integration_live.py`, sentinel `fiscal_year=2094` — 2095/2096/2097/2099 already taken) — 38/38 live pass, 0 residual rows. 67 new frontend tests — 251/251 pass (was 184), `npm run build` green. Playwright headless verify (mocked API, one persona per scenario): 6/6 PASS (submit DRAFT→PENDING_APPROVER1, approver sees Approve+Reject and approves→APPROVED, reject with required-reason textarea, รออนุมัติ badge shows, admin toggle switches `admin_view_enabled` on refetch, no-scope empty state), 0 console errors; light+dark screenshots saved to `%TEMP%` (`a10_main_light.png`, `a10_approver_dark.png`) — user to review. NOT built: dashboard (Phase 2), deploy. `.env` NOT edited, NOT committed (per instructions).
  - [x] **A10 gap closed 2026-07-16 — department-approval lock on the write path** (the flag above): `backend/app/write_model.py` — new `DepartmentLockedError`→`department_locked`/403 (registered in `ERROR_HTTP_STATUS`) + `_ensure_department_not_locked` (mirrors `_ensure_not_post_deadline`'s pattern exactly: admin bypasses first, checked before every DB write) — non-admin writes/deletes to a `(department, fiscal_year)` that is `PENDING_APPROVER1/2/3` or `APPROVED` in `budget.approval_status` are rejected per-item; `REJECTED`/no-row = allowed (unchanged), matches ADR-0013's edit-rights table exactly. Wired at all 5 write entry points (`_save_one_pending_row`/`_save_one_detail_line`/`_save_one_trip`/`_delete_one_detail_line`/`_delete_one_trip`), same call sites as the deadline gate. Department resolved via `dbo.cc_filler_map` (`_lookup_cc_dims`, same D11 deterministic tie-break) — reused from the already-computed `dims["department"]` for pending-row/detail-line saves (0 extra queries there), freshly looked up for trip/delete paths (1 extra query) via a proper sentinel default (`_UNRESOLVED_DEPARTMENT`, NOT `None`) so a cost_center with no resolvable department is never redundantly re-queried. **Unknown CC→department mapping decision**: treated as "not locked" (fail-open, mirrors `is_post_deadline`'s own missing-row policy) — cannot currently be reached by a non-admin since their Fill scope is itself derived from the same map. 30 new mocked tests in `test_write_model.py` (111/111 total pass, was 81) + 18 existing tests updated for the new legitimate DB call (mock sequencing only, no weakened assertions). 3 new live tests in `test_integration_live.py` (sentinel `fiscal_year=2093`, distinct from 2094–2097+2099 already used) using the REAL `submit_department` state machine: non-admin edit blocked (`department_locked`, value unchanged in DB), admin edit succeeds, non-admin delete blocked — all 3 green against the real Fabric SQL DB, 0 residual rows verified (39/39 live pass in the file). **Frontend**: `frontend/src/api/client.ts` — `messageForStatus` now takes the backend `detail` string and special-cases the ONE stable marker phrase from `DepartmentLockedError` ("mid-approval or approved, editing is locked") to show "ฝ่ายนี้อยู่ระหว่างรออนุมัติ/อนุมัติแล้ว — แก้ไขไม่ได้" instead of the generic forbidden message; a plain Fill-scope 403 is unaffected. 2 new tests in `client.test.ts` — 254/254 frontend tests pass, `npm run build` green. `.env` NOT edited, NOT committed (per instructions).
  - [x] **Permanent Playwright E2E journey suite added 2026-07-16** (jakkaritw explicit order, replacing an earlier page-load-only smoke suite): `frontend/playwright.config.ts` (chromium headless, `workers: 3` — capped deliberately, a higher count throws too many browser contexts at the single shared Vite dev server on first navigation and produced flaky "setting up page" timeouts in practice) + `frontend/e2e/fixtures.ts` (persona route-mock helpers — filler/approver/dual-role-admin/pure-admin/no-scope — intercepting every `backend/app/routers/*` endpoint this SPA calls at the network layer via `page.route()`, contract-accurate to `src/api/types.ts`; a console-error collector fixture fails any test on an unexpected `console.error`, allowlisting only Chromium's own "Failed to load resource: status NNN" diagnostic that fires whenever a mocked 4xx/5xx response is deliberately served) + 4 spec files (`filler-journey`/`approver-journey`/`admin-journey`/`edge-states.spec.ts`, 23 tests total, real payload/DOM assertions per step, not page-load smoke). `npm run test:e2e` runs it; `vite.config.ts` test.exclude keeps Vitest off `e2e/**`. **1 real frontend bug found + fixed while writing this** (not masked): `src/api/client.ts`'s `messageForStatus` special-cased ONLY `DepartmentLockedError`'s 403 detail marker — a `PastDeadlineError` 403 (same HTTP status, `write_model.py`/`approval.py`) fell through to the generic "ไม่มีสิทธิ์เข้าถึงข้อมูลนี้" (permission denied) message, which is misleading (the user DOES have permission, the cycle is just closed) — added a second stable-marker check (`client.test.ts` +1 regression test, TDD RED confirmed first) mapping it to "พ้นกำหนดส่งงบประมาณของปีนี้แล้ว — กรุณาติดต่อผู้ดูแลระบบ". 255/255 vitest pass (was 254), `npm run build` green, 23/23 Playwright pass. **CI note**: `.github/workflows/` NOT touched (browser provisioning on CI runners needs `npx playwright install --with-deps chromium` added to whichever workflow eventually runs `frontend/`; deferred, no such workflow exists yet). Not committed (per instructions).
  - [x] **A14 step 2 (deploy artifacts) done 2026-07-16**: `backend/Dockerfile` (multi-stage node:24-alpine build → python:3.12-slim-bookworm runtime, msodbcsql17, non-root user, `uvicorn --workers 2`, STATIC_DIR=/app/static for the parallel StaticFiles+SPA-fallback work) + root `.dockerignore` + `docs/deploy/A14_RUNBOOK.md` (Cloud Shell paste-ready: resource discovery, `az acr build`, staging-then-production Container Apps, Easy Auth incl. `/docs`, verify-deploy-landed, rollback, control-number reconcile note) + `.github/workflows/budget-app-deploy.yml.DISABLED` (future CI stub, inert). **NOT executed/deployed** — no az/docker run locally (impossible per no-install constraint); artifacts only, awaiting 07-security-auditor pass + jakkaritw's explicit go-ahead before anyone runs the runbook in Cloud Shell. Flagged in the runbook: `backend/requirements.txt` still unpinned; staging shares the same `fabric_sql_database` as prod (no separate staging DB exists yet).
  - [x] **A14 gate finding fixed 2026-07-16 — path traversal in SPA fallback (CWE-22)**: `backend/app/static.py`'s `spa_fallback` did `static_dir / full_path` and served it via `FileResponse` with no containment check — a URL-encoded `..` (`%2e%2e`, decoded to `..` by the ASGI layer before the app sees it) let `GET /%2e%2e/secret.txt` read any file outside `frontend/dist` the container user could access (proven live: 200 + file contents). Fixed with `_is_contained(candidate, static_dir)` — resolves both paths and checks containment via `resolved.parents` before serving; an out-of-bounds candidate now falls through to the SAME existing rule as a missing file (extension-less → index.html SPA route, extension-bearing → 404), never served raw. Covers single/double/deep `%2e%2e` encodings and a backslash variant uniformly (resolve-based, not a string blocklist); `/assets` mount and reserved-prefix behavior untouched. TDD: RED confirmed first (4/5 new exploit tests returned 200 + leaked secret before the fix — 1 raw-`..` vector was already blocked client-side by httpx's own normalization, kept as a documented defense-in-depth case), GREEN after. 7 new tests in `backend/tests/test_static.py` (14/14 file, was 7); full suite `pytest tests -m "not integration"` 500/500 pass (was 493). NOT committed (per instructions) — 07-security-auditor pass on A14 still needed before deploy.
  - [x] **A14 clean-build fix 2026-07-16 — `az acr build` was failing**: `npm run build` (`tsc -b && vite build`) type-checked test files too, and `src/subform/glDropdownConstants.test.ts` imports `../../../docs/reference/special-gl-dropdown-fixture.json` — a path outside `frontend/` that the Docker build context (`.dockerignore` prunes root `docs/`) never has, so the cloud build died with `TS2307 Cannot find module`. Fixed by adding an `exclude` block to `frontend/tsconfig.app.json` (`src/**/*.test.ts(x)`, `src/**/*.spec.ts(x)`, `src/test/**`, `e2e/**`) so the PRODUCTION tsc project never sweeps test files; Vitest is unaffected (it discovers/transpiles tests via its own `test.include`/`test.exclude` in `vite.config.ts`, not this tsconfig). Verified: `npx tsc -b --listFiles` now matches 0 test/spec files and 0 fixture files (was 26/1); `npm run build` green with `docs/` temporarily renamed away (simulates the container exactly), `docs/` restored immediately after, 0 diff; `npx vitest run` still 255/255 green incl. the fixture-parity test; e2e specs confirmed excluded from the build (were never swept in the first place — they live outside `src`, both `tsconfig.app.json`/`tsconfig.node.json` only include `src`/`vite.config.ts`). No other outside-`frontend/` import found (repo-wide grep). Not committed (per instructions).
  - [x] **Fix: `GET /budget/detail` + `GET /budget/trip` 500 on any non-empty result (2026-07-16)** — `backend/app/routers/subform.py`'s `DetailLineRow`/`TripRow` typed `updated_at: str`, but pyodbc returns a real `datetime`, and Pydantic v2 doesn't coerce datetime→str — every non-empty read raised an uncaught `ValidationError` (router only catches `pyodbc.Error`). Broke reopening an existing detail line/trip for edit AND the optimistic-lock token (`expected_updated_at`) needed for any UPDATE — create-only worked, edit was fully broken. Fixed: `updated_at: datetime` on both models (matches write-side `TripState`/`DetailLineState`); FastAPI serializes to ISO8601 automatically, so the JSON contract is unchanged (frontend already treats it as a string). TDD: RED confirmed first (4 new tests in `backend/tests/test_subform_router.py` — 2 model-level `DetailLineRow(**row)`/`TripRow(**row)` construction, 2 router-level via TestClient with a real `datetime` mocked in `fetch_detail_lines`/`fetch_trips` — all failed with the exact diagnosed `ValidationError` before the fix), GREEN after. Full backend suite: 512 passed / 44 deselected (was 508 passed).
  - [x] **2 backend fixes 2026-07-16 — read-path 502 contract + security headers**: (FIX 1) `resolve_scope` ran OUTSIDE the try/except in every read router — a pyodbc.Error during RLS scope resolution propagated uncaught (HTTP 500), violating the "Fabric SQL failure on a read = 502" contract. Moved inside the existing `except pyodbc.Error → 502` in `routers/budget.py` + `routers/subform.py` (both endpoints) + `routers/reference.py` (`/scope/departments`); `routers/scope.py` had NO handler at all — added one (same generic `_DB_UNAVAILABLE_DETAIL`, `logger.exception` pattern). Admin-bypass/scope logic and the 403 `_ensure_read_scope` path unchanged (HTTPException passes through the pyodbc-only except). (FIX 2) `main.py` — named `add_security_headers` `@app.middleware("http")` setting `X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy: strict-origin-when-cross-origin`, `Strict-Transport-Security: max-age=31536000; includeSubDomains` on EVERY response (API, errors, /health, SPA); Content-Security-Policy deliberately deferred (would break the Vite/React SPA) — pinned by a test so adding it later is an explicit change. TDD: RED confirmed first (5 scope-resolution tests raised uncaught pyodbc.Error; security-headers file failed collection on ImportError), GREEN after. 9 new tests (test_budget +1, test_scope +1, test_subform_router +2, test_reference_router +1, NEW test_security_headers.py 4 incl. SPA-route via isolated tmp-dist app) — full suite 522 passed / 44 deselected (was 513). Known residual: `get_fabric_conn()` failing at connection-OPEN (before the with-body) still 500s — out of this fix's scope, flag if it bites (CLOSED 2026-07-16, two items below). Dockerfile/db.py/write_model.py untouched (parallel fix owns them). NOT deployed, not committed (per instructions).
  - [x] **POST /budget/trip idempotency (client_token dedup) done 2026-07-16**: network retry / double-click can no longer duplicate a trip or double-count per-diem. Backend (`write_model.py`): `TripInput.client_token` (optional, ≤64), CREATE-only dedup — pre-INSERT `SELECT ... WHERE client_token=? AND _user=?` replays the STORED trip (no INSERT, no per-diem re-add, no commit; per-diem recomputed-on-read per ADR-0015) + `pyodbc.IntegrityError` on the tokened INSERT → rollback → re-SELECT → replay (true concurrent double-submit); token-less INSERT stays byte-identical to pre-migration (safe against the un-migrated live table); PUT never dedups. Dedup key = (client_token, _user) ONLY — content-unique deliberately rejected (two identical trips are legitimate). Frontend: `blankTripDraft` generates `crypto.randomUUID()` once per new-trip card (kept across error retries, regenerated per card), `draftFromTripListItem` → null; save button already disabled in-flight (regression-locked by test). Schema: `db/ddl/budget_transactional_tables.sql` + spec DBML updated; **live ALTER NOT run** — `setup/migrate_budget_trip_client_token.py` (SP pyodbc, ODBC 17, INFORMATION_SCHEMA/sys.indexes-checked idempotent, dry-run default, `--apply` to execute) awaits jakkaritw's go. Tests: backend 522→534 mocked pass (12 new, RED-first), frontend 255→261 (6 new), `npm run build` green, e2e 23/23. **Live integration trip tests will keep passing pre-migration** (no token sent by them), but the deployed frontend ALWAYS sends a token → migration MUST be applied before the next frontend deploy.
  - [x] **Connection-OPEN failure → 502 contract closed 2026-07-16** (the "Known residual" flagged two items up, gate-confirmed by staging): a failure at `with get_fabric_conn()`/`get_gold_conn()` entry (driver pyodbc.Error e.g. HYT00/capacity pause, OR msal token failure) used to propagate uncaught → HTTP 500 on most DB-backed endpoints. Fix = approach B (typed error at the source): `backend/app/db.py` new `DbConnectionError(pyodbc.Error, RuntimeError)` raised by `_acquire_access_token` instead of raw RuntimeError — every router's existing `except pyodbc.Error → 502` now covers open-time msal failure with zero except-broadening, while the RuntimeError parent keeps health.py's deep-check catch + test_db's surface valid; success path (msal token injection) byte-identical. Routers where the `with` sat OUTSIDE the try got it moved inside: `routers/budget.py`, `scope.py`, `subform.py` (×2), `reference.py` (×2), `budget_write.py` (×5 endpoints + `_save_one_trip` — this also closed the residual query-time gap where `resolve_scope` sat outside the try in ALL write endpoints). `approval.py`/`attachments.py` (open already inside `_run`) and `health.py`/`me.py` untouched. Approach A (catch RuntimeError in routers) rejected — this codebase uses RuntimeError subclasses as business errors (`ConcurrentApprovalError`→409, `MissingFxRateError`→500, `SapActualsFetchError`), broadening would corrupt those mappings into 502s. TDD: NEW `backend/tests/test_conn_open_502.py` — 14 endpoints × 2 failure modes (driver-open via patched router `get_fabric_conn`; msal-open via patched `app.db.msal` with the REAL `get_fabric_conn` running end-to-end), leak assertions (no HYT00/AADSTS/msal/ODBC text in body); RED confirmed first: 25 failed/3 passed (3 = approval submit/status + attachments driver-open, already wrapped — kept as contract locks). +1 unit test in `test_db.py` locking the dual-parent surface. Full suite 563 passed / 44 deselected (was 534). 401 auth + 403 business behavior unchanged. NOT deployed, NOT committed (per instructions).
  - [x] **2 frontend UX fixes 2026-07-16 (frontend/ only)**: (FIX 2) modal-load click race — `DetailSubform.tsx`/`TripManager.tsx` "+ เพิ่มรายการ"/"+ เพิ่มทริป" now `disabled={loading}` (disable not hide, matches the existing in-flight-disable pattern; a click before `load()` resolved was silently lost when `setRows`/`setCards` replaced the array; also covers ลองใหม่-retry + TripManager's 409 refetch for free). (FIX 3) 422 no longer renders bare "คำขอไม่สำเร็จ (HTTP 422)" — `api/client.ts` parses FastAPI/Pydantic `detail` arrays ({loc,msg,type,ctx}) into "ข้อมูลไม่ถูกต้อง: <field> — <Thai reason>" (first 2 entries + "และอีก N รายการ"; type map grounded in `write_model.py`'s real constraints — missing/ge/le/lt/string_too_long/parsing/literal; unmapped type falls back to the entry's English `msg`; string/absent detail keeps the generic message; `ApiError.detail` stays string-only; 403/409/400/5xx byte-identical). TDD RED-first (7 failing 422 msgs + 2 failing toBeDisabled), 9 new tests — 261→270 vitest pass, `npm run build` green, oxlint no new issues, committed e2e 23/23 each passed (3 admin-journey tests flaked once under concurrent-session load — known mode, green on re-run; 1 unattributed transient vitest flake in one full run, 2 subsequent full runs clean). Not committed (per instructions).
  - [x] **Trip side (ฝั่งบัญชี) derived from ฝ่าย-level history 2026-07-16 (frontend/ only, decided by jakkaritw)**: `blankTripDraft`'s blind `side:'SGA'` removed — side now comes from the ฝ่าย's REAL travel-GL history (8 GLs, `sap.total_year`/`board.total_year` nonzero; pending never counts) aggregated across ALL sibling CCs of the trip CC's ฝ่าย (refined grain per live data: 86 ฝ่าย w/ history = 54 SGA-only/19 COST-only/13 BOTH). Rule: exactly one side → hard-lock the select for non-admins (Thai tooltip); both → enabled, default = larger summed total (tie→SGA, org majority); none → placeholder "— เลือกฝั่ง —" + save button disabled until picked (no silent default, `validateTripDraft` belt-and-braces + `buildTripPayload` throws on null side); admin (`scope.isAdmin`) → always enabled. Data source = ZERO new fetch/endpoint: pure `deriveTravelSideHistory(rows, deptCCs)` in `subform/model.ts` over the parent grid's already-loaded `/budget` rows (dept filter is server-side under the same RLS a refetch would use, so a refetch adds nothing), CC→ฝ่าย via the loaded `/scope/departments` + existing `costCentersOfDepartment`; unmapped CC degrades to own-rows-only. `TripDraft.side` now `TripSide|null`; `TripManagerProps` +`sideHistory`+`isAdmin` (component stays grain-agnostic). TDD RED-first (17 failed/64 passed in the 3 touched files), 270→287 vitest pass (17 new: 11 model incl. ฝ่าย-inheritance/larger-total/pending-not-history, 5 TripManager select states, 1 BudgetGrid ฝ่าย-grain integration), `npm run build` green. Playwright headless (real Chromium, mocked routes, vite dev): 8/8 asserts PASS across 2 scenarios (sibling-CC SGA inheritance locked; no-history must-pick → POST carries picked side), 0 console errors — logic change, no screenshot per policy. Backend NOT touched: server still accepts any side on POST/PUT `/budget/trip` — flag for write-path owner whether to enforce the ฝ่าย-history rule server-side (client lock is UX, not security). Not committed (per instructions).
  - [x] **Non-master GL rows read-only in the grid (2026-07-16, jakkaritw rule)**: pending cell editable ⟺ `row.editable` (Fill scope, server flag unchanged) AND GL ∈ master (`GET /budget/gl-accounts`) — closes the live trap where ~106 SAP-led GLs outside the 142-account master rendered inputs that PUT /budget/rows 400'd ("not a recognised GL account", add-later policy: admin adds via Edit GL Group → row becomes editable automatically on next master load, no special-casing). `grid/model.ts` `GlMeta.in_master` + 3-arg `isEditableCell`; `GridTable.tsx` read-only reference marker "อ้างอิง — ยังไม่เปิดให้ตั้งงบ" (Fill-scope rows only; See-only rows unchanged) + tooltip on cells; `.reference-hint` CSS (muted `.special-hint` sibling). TDD RED-first (5 failed), 287→291 vitest pass, `npm run build` green, committed e2e 23/23, throwaway Playwright verify PASS (screenshot in scratchpad, spec deleted). Not committed (per instructions).
  - [x] **Trip-subform backend gaps (travelers / countries / trip.project / free-text plate) done 2026-07-17 (backend only, parallel frontend agent owns frontend/)**: `GET /reference/travelers?cost_center=` (See-scope gated, admin bypass; source = `dbo.v_employee_primary` FULL roster — dept→employee mapping live-proven unreliable: `cc_filler_map.department` matches `org_name_en` for only 35/114 ฝ่าย, task-approved fallback; same view `_lookup_traveler` validates, so every pickable traveler is save-able) + `GET /reference/countries` (identity-only auth; live `dbo.country_group` stores NAMES 'domestic'/'asian' — introspected, spec DBML wrong — mapped to ints 1/2, group 3 "other" is frontend-added; unknown group name → warn+skip) in `reference_data.py`/`routers/reference.py`. `TripInput/TripState/TripRow.project` (≤200, template col F) persisted via the client_token conditional-column trick (None path byte-identical legacy SQL — write path deploy-safe pre-migration) + returned by `subform_read.fetch_trips`; **`fetch_trips` SELECTs `project` unconditionally → `setup/migrate_budget_trip_project.py` (authored, NOT run, dry-run default) must run BEFORE the next backend deploy or GET /budget/trip 502s** (client_token migration has the same pending status). `purpose` confirmed already end-to-end. `special_gl.validate_lease_meta`: vehicles ทะเบียนรถ = free text (jakkaritw 2026-07-17) — any non-empty ≤50-char string; sentinel strings "อื่นๆ"/legacy "ไม่ระบุ" rejected (never persisted); `_LEASE_PLATES` fixture-parity constant renamed to 7 plates + "อื่นๆ" matching the shared fixture (frontend agent renamed the JSON side same day). DDL + spec DBML updated. TDD RED-first per deliverable; backend suite 563→595 mocked pass (+32), fixture parity green, live read-only smoke: 13 countries (groups 1/2), 612 travelers sorted, 0 empty fields. Note for frontend: vite dev proxy needs the NEW `/reference` namespace forwarded. `.env` NOT edited, NOT committed (per instructions).
  - [x] **A9 subform dropdowns v2 (frontend only, 2026-07-17, jakkaritw approved)**: `TripManager.tsx` — traveler = dropdown from `GET /reference/travelers?cost_center=` (label ชื่อ, value empcode, placeholder "— เลือกผู้เดินทาง —"), position = read-only auto-display (traveler master wins, trip response fallback for a delisted empcode, `data-testid=position-*`), destination = country dropdown from `GET /reference/countries` + client-appended "อื่นๆ (Other)"→group 3 that AUTO-SETS `country_group` 1/2/3 (manual กลุ่มปลายทาง select REMOVED — financial-correctness fix; destination now REQUIRED client-side since group derives from it; legacy free-typed destination/traveler render as fallback options, stored group kept until re-picked), new `project` + existing `purpose` text inputs (TripInput/TripState/TripListItem + `project: string|null`). `DetailSubform.tsx` — Lease Vehicles (…060) ทะเบียนรถ trigger renamed ไม่ระบุ→**อื่นๆ** (mid-flight spec change): picking it reveals a REQUIRED free-text plate (placeholder "พิมพ์ทะเบียนรถ"), typed plate is what gets sent (never the literal), blank blocks save with "กรุณาพิมพ์ทะเบียนรถ" and NO API call, custom plate round-trips (select อื่นๆ + prefilled box); shared fixture `docs/reference/special-gl-dropdown-fixture.json` plates entry updated ไม่ระบุ→อื่นๆ — **backend parity test + `special_gl.py` must follow (parallel backend agent)**, backend must also return `project` on trip GET/POST/PUT responses + serve the 2 new `/reference/*` endpoints. New `frontend/src/api/reference.ts`; vite dev proxy + e2e fixtures gained `/reference`. Preserved: 12-month picker, side history-lock, client_token idempotency, in-flight disable, 422 mapping. TDD RED-first (module 4 + model 13 + component 9 failures confirmed). 291→319 vitest pass (28 new), `npm run build` green, oxlint clean, committed e2e 23/23 (fixtures + filler-journey 1.8 updated to the new dropdown flow), throwaway Playwright verify 11/11 PASS 0 console errors (screenshots `verify_trip_manager.png`/`verify_lease_plate.png` in session scratchpad — user to review). Backend NOT touched, NOT committed (per instructions).
  - [x] **Gate blocker fixed 2026-07-17 — trip `project` clear-field bug (both halves)**: (1) `frontend/src/subform/TripManager.tsx` project `onChange` sent `e.target.value || null`, so clearing the field on an EXISTING trip sent `project: null` → backend's conditional-column UPDATE (`project = ?` only when not-None) left the OLD value in place, stale value reappeared on reload; fixed to send `e.target.value` (a real `""` on clear) — `purpose` untouched (no such semantics), `buildTripPayload`/drafts already passed strings through uncoerced. (2) `backend/app/write_model.py`'s immediate save response echoed the REQUEST'S `trip.project` (so a None-UPDATE echoed null while the DB kept the old value); added `_lookup_trip_project` (new helper, SELECT `project` by trip_id) called ONLY when `trip.project is None and trip.trip_id is not None` (update, column left untouched) — a project-less CREATE still echoes None with no extra read (column was never written). TDD RED confirmed both halves (frontend: `expected null to be ''`; backend: strengthened test caught `assert None == 'Legacy Project'`) via temporary revert-and-rerun, then restored. 4 tests added/strengthened: `test_trip_update_with_empty_project_clears_the_column` (new), `test_trip_update_without_project_keeps_the_legacy_update_shape` (strengthened to assert the echoed value, not just "no project in SQL"), 2 unrelated pre-existing trip-update tests needed one more mocked fetchone entry (my new read-back call executes whenever project is omitted on an update, not just in the 2 targeted tests). Backend 595→596 mocked pass, frontend 319→320 vitest pass, `npm run build` green. Docs: `docs/specs/budget-transactional-data-model.md` — 1-line deploy-ordering note added (both GET and PUT /budget/trip 502 pre-migration, not just GET). Not committed (per instructions).
  - [x] **A1 — past-deadline lock PROVEN live on staging + real Fabric SQL DB, 2026-07-17 (test-only, no prod code changed)**: seeded `dbo.submission_deadline` fiscal_year=2032 with `deadline_date=2020-01-01` (past), all 9 NOT-NULL cols filled per live `INFORMATION_SCHEMA.COLUMNS`. Against staging (`cman-budget-web-stg...`, Easy Auth OFF, `x-ms-client-principal-name` impersonation) as real filler `anurakb@chememan.com` (CC `10MN012100`): `PUT /budget/rows` FY2032 → **403** `{"detail":"the submission deadline for fiscal_year=2032 has passed"}`; `PUT /budget/detail` FY2032 (Entertainment special-GL line) → same 403 (subform path locked too, same `_ensure_not_post_deadline` call site); `GET /budget?year=2032` → 200 (read path stays open while locked); negative control `PUT /budget/rows` FY2031 (no deadline row seeded) → 200 success (row created) — proves the lock is deadline-specific, not a blanket future-year block. Cleanup + reconcile: 0 residual rows in `budget.pending_budget`/`pending_budget_detail`/`budget_trip` at FY2031+FY2032, `dbo.submission_deadline` FY2032 row deleted, all verified via `SELECT COUNT(*)` after DELETE. **Finding (not a bug, a spec-vs-reality gap):** the lock message is English only ("the submission deadline for fiscal_year=... has passed") — `write_model.py`'s `_ensure_not_post_deadline`/`PastDeadlineError` never had a Thai string; the ONLY Thai translation lives client-side in `frontend/src/api/client.ts`'s `messageForStatus` marker-match (confirmed present, `A9`/dedup entry above). Backend `detail` field is English-only by design (matches every other error in `ERROR_HTTP_STATUS`) — flagging only because this task's brief assumed a backend Thai message; no code changed. `POST/PUT /budget/trip` was NOT independently HTTP-tested (would need a real traveler/per-diem-rate/FX fixture chain) but `write_model.py:1313` shows the identical `_ensure_not_post_deadline(conn, trip.fiscal_year, scope)` call at the same point in `_save_one_trip` — same code path, provable by inspection, not separately exercised live.
  - [x] **A2 — real-browser smoke of deployed staging build (budget-web:cc40d39), 2026-07-17 (verification only, no code changed)**: Playwright headless Chromium against `cman-budget-web-stg...` (Easy Auth OFF), origin-scoped `x-ms-client-principal-name: anurakb@chememan.com` header via `ctx.route` (not `extra_http_headers`, per memory gotcha — no cross-origin calls exist anyway, FastAPI serves the SPA same-origin, A14). **6/6 checks PASS, 0 console errors, 0 failed network requests (4xx/5xx) across the whole run:** (1) SPA loads at `/`, `.page-title`="OPEX Management", user-bar shows `anurakb@chememan.com` (Fill=5 CC, See=5 CC — real RLS data, broader than the brief's 1-CC assumption); (2) 0 console/network errors; (3) main grid renders 26 real txn-blocks (SAP+Approved+Pending 3-layer) for the default planning year 2027, no empty/error state; (4) special-GL subform (Travelling Expense, found via a read-only YearPicker fallback scan — the default year 2027 had zero special-GL rows for this persona, FY2025 did) opened `TripManager` and rendered ALL new fields: traveler dropdown, read-only position, destination country dropdown, Project + Purpose inputs; (5) reference dropdowns proven populated from the REAL API through the served SPA: 613 traveler options (`/reference/travelers`), 15 country options (`/reference/countries`); (6) SPA fallback confirmed — an unknown deep-link path (`/deep-link-smoke-check-a2`) returned 200 `text/html` (index.html, not 404) and the app re-rendered; back-to-root also clean. **1 real-data gap, not a bug**: no Lease & Rental (any suffix, incl. Lease Vehicle `...060`) row exists anywhere in anurakb's 5 Fill-scope CCs across FY2025–2028 (confirmed via a full year-by-year scan), so the ทะเบียนรถ dropdown could not be exercised live for this persona — it IS a static frontend list (`LEASE_PLATES` incl. `อื่นๆ`, not server-driven), so this gap carries no deploy risk, just no live persona to demo it with; recommend a persona/CC with real lease-vehicle history for a full demo. No DB writes (read-only smoke; year-picker changes are GETs only). Screenshots: `%TEMP%\a2_1_loaded.png` (grid loaded), `%TEMP%\a2_2_trip_manager.png` (Travelling Expense subform open) — user to review. Throwaway scripts deleted after run per policy.
  - [x] **UI iteration #1 — header (V3 user-bar) ported to real React, 2026-07-17 (frontend only, backend untouched)**: `App.tsx`'s plain `UserBar` (email + role badge + Fill/See CC chips) rewritten as a faithful port of the mockup's V3 login bar (`design/mockups/0002claude design/0002.3budget-export.html` `.user-bar.v3` + `renderUserBar()`) — avatar (initials from email local-part) + name/email/ONE Thai role badge, then `สายงาน › ฝ่าย(n) [chips] › Cost Centers(n) pill / GL Codes(n) pill`. New `frontend/src/userbar/` module: `model.ts` (`deriveScopeSummary`/`countDistinctFillGlAccounts`, pure), `useOwnDepartments.ts` (`GET /scope/departments` always `admin_view_enabled=false` — the header shows the caller's REAL personal scope regardless of `BudgetGrid`'s admin-view toggle), `useFillGlCount.ts` (`GET /budget?year=` with no department filter, filtered client-side to Fill CCs, distinct `gl_account` — reuses the EXISTING endpoint, no backend change), `UserBar.tsx`. **Design decisions**: See-scope count REMOVED from the header (Fill-only, jakkaritw's call); mockup's user-switcher omitted (no persona to switch, real Entra auth); pure admin (ADR-0014, no Fill/See CCs) shows the mockup's "เห็นข้อมูลทั้งหมด" note, a dual-role admin (Nipaporn/Waraporn) still shows their own real hierarchy; GL count is a SEPARATE fetch from `BudgetGrid`'s own grid load (not literally shared/deduped — flagged as a follow-up, see below), shows "…" while loading. **Known tradeoff flagged, not fixed**: adds one more `GET /budget` call per page load (same expensive whole-company SAP-scan query the B6 perf note already flagged, ~800-1200ms) — deemed acceptable for a header metric per the task brief's "brief moment '…' then the number" allowance, but is a real additional backend round-trip; candidate follow-up = push RLS cost_center filtering into `SAP_ACTUALS_SQL` (same B6 recommendation) which would make this and every other `/budget` call cheaper. CSS ported into `frontend/src/styles/global.css` (`.user-bar.v3`/`.v3-*`/`.user-avatar`/`.meta-tag`/`.role-badge.user`+`.admin`) reusing existing tokens (no new tokens needed, `--serif` etc. already present); old non-V3 `.user-id`/`.user-chip`/`.user-chips`/`.role-badge` (single-variant) CSS deleted as dead code. 320→344 frontend tests pass (24 new: model/hook/component), `npm run build` green, `npx oxlint` clean on touched files. Not screenshotted/not verified live per the task's explicit instruction (jakkaritw reviews live at the running dev server, 127.0.0.1:5180, hot-reload). Not committed (per instructions).
  - SAP actuals = read-through DW `cman_dw_wh_gold.gold.fact_gl_trans` ws 302668d3 (ADR-0020): DW-side GROUP BY → FastAPI merge. **VERIFIED 2026-07-13 (investigation, read-only probe):** SP `cman-fabric-write` access to ws 302668d3 now WORKS (prior 2026-07-09 block is resolved — update gotcha memory). Table IS live, 23.7M rows, but is the OLD pre-"CORRECTION 3" dimensional-fact build from 19.dw_jakkaritw's history (94-col, `period_month`/`prcs_*` audit cols) — NOT the current dev-repo spec (which renamed gold→`sap_gl_trans` pure-mirror and is dormant/undeployed). Confirm with DW owner who/what refreshes this PRD object before depending on it long-term. Verified: `company_curr_amount` already correctly signed by `debit_credit_ind` (S+/H-, ~97% consistent on real 2026 data — do NOT apply the old data-sources.md "flip sign on H" rule, that was for a different manual export); reversal pairs (`reversing_flag`/`reversed_flag`/`true_reversal_flag`) net to exactly zero when summed — no reversal filter needed; MUST filter `company_code='1000'` (CMAN-TH/THB only — table also holds 2000=CMAN-AU/AUD, 9001=HLL/VND, 4000=GMAN/THB) and `doc_type<>'CO'` (CO Posting = 19% of rows, 2.15M carry cost_center, double-counts vs FI side) and the data-sources.md cost-center exclusion list (`10SC012000,CMRY01,CMKK01,CMPB01,MNLB00..04` — all confirmed present in real data) + `assignment_number<>'TFRS16'`. Grain = `(cost_center, gl_account_number, fiscal_year, period_month)`. **RESOLVED 2026-07-14 (read-only probe, GUESS — needs DW/accounting confirm):** for company_code='1000', `doc_status` has only 2 values: NULL (17.1M rows, net -981,288.90 THB — the real actuals) and `'U'` (4.55M rows, net **exactly 0.00 THB**). 98.05% of `'U'` rows are `doc_type='CO'` (already excluded by the existing `doc_type<>'CO'` rule); the residual 1.95% (88,741 rows) are MM goods-movement doc types (WL/WA/WE/WI/SF) that ALSO net to 0.00 as a group. Concrete check at real grain (cost_center=10CA013000, gl=6211700020, FY2023/mar): including vs excluding `'U'` rows changed row count (235→1314) but net SUM was identical (578,997.17 both ways) — `'U'` rows there summed to 0.00. Only ~20% of `'U'` rows carry an explicit reversal-pair flag, so it isn't purely "reversed docs"; best guess is `'U'` = statistical/self-balancing entries (CO internal allocations + MM goods-movement memo postings), not real unposted/parked FI docs — date range is full history (2019–2026), not recent-only. **Practical takeaway: excluding vs including `doc_status='U'` should not move any SUM in this table** — but this is a GUESS from data patterns only, not a confirmed SAP status-code definition; still get DW/accounting to confirm before hard-coding "safe to include" anywhere. Latest data = FY2026 period 04 (Apr) — mind the lag.
  - main-table read path (grill 2026-07-12): visible-row union key = `(cost_center, gl_account)` (layers carry different years: SAP=Y, Approved=Y, Pending=Y+1); Approved-Y = reference only; board+pending join inside Fabric SQL DB, SAP merged cross-store in FastAPI; requested-vs-granted = Phase-2 dashboard
  - approval engine (grill 2026-07-12, ADR-0006/0008): `approval_status` keyed (ฝ่าย, fiscal_year) with frozen approver1/2/3 empcodes + last_submitter_empcode snapshotted at Submit; reject at ANY step → REJECTED (editable) → resubmit restarts whole chain at PENDING_APPROVER1; **all automations are Phase-1**: (A) reject→email to last submitter, (B) auto-submit DRAFT ฝ่าย at deadline, (C) 30-day auto-escalate, (D) pre-deadline reminder email on `reminder_date` to Fillers of still-DRAFT ฝ่าย (grouped one email/Filler listing their pending ฝ่าย, submitted excluded) → needs scheduled jobs (GitHub Actions cron like sync_employees, or Azure Function timer) + Graph sendMail
- [ ] test env → prod folder (TBC)
- [ ] follow-up doc sweep: `.claude/project-context.md` + `docs/reference/data-platform-map.md`/`data-sources.md` still say `gold_sap_gl_trans`; mockup 0002.2 + signoff doc 01 still show the dropped CSV import/export buttons
- [x] **UI-parity fix series, point 2 — row-tint CSS ported to React grid (2026-07-17, frontend CSS only)**: `frontend/src/styles/global.css` — copied the mockup's 3 row-tint rules (`0002.3budget-export.html` lines 574-577) verbatim into the grid block, right after the point-1 frozen-column rules (`.data-table tr[data-status="sap"|"approved"|"pending"] td:not(.shared)`, specificity 0,3,2 beats `.data-table td.frz`'s 0,2,1, so the 3 frozen identity cells pick up the same tint — no untinted stripe). Headless Playwright verify (mocked API, real Chromium): 9/9 assertions PASS — sap/approved/pending rows each tinted vs plain `--surface`, each FROZEN td's background matches its row's plain td exactly, 3 distinct washes, subtotal-row frozen td unchanged (`--paper-2`, no `data-status` so untouched), 0 console errors. `npm test` 344/344 green (untouched, CSS-only). Screenshot saved (not read) to scratchpad `point2_row_tint.png` — awaiting jakkaritw visual review before point 3. Point 1 (frozen identity columns) and point 3 (whatever's next in the series) are NOT tracked as their own plan.md lines — flagging for the parent session to fold into this series entry or its own tracker if it wants full point-by-point history.
- [x] **UI-parity fix series, point 3 — month-value pill coloring by data layer (2026-07-17, frontend only)**: `frontend/src/grid/GridTable.tsx` — `MonthCells` gained a `variant: 'sap' | 'approved-ro'` prop (SAP row passes `sap`, board/Approved row passes `approved-ro`); each month's value now renders as `<span className="month-value {variant}{ zero if ===0}">` INSIDE the unchanged `<td data-testid>` (test ids untouched). `frontend/src/grid/MonthCell.tsx` — read-only branch span gained `pending-readonly` + conditional `zero`; the editable `<input>` branch untouched (still plain `month-value month-input`, no pill). `frontend/src/styles/global.css` (~lines 793-846) — base `.month-value` updated to mockup values (font-weight 500, padding 4px 8px, border-radius var(--r), min-width 60px) with NO background (only variant classes paint background), so `.month-input`'s own later-declared padding/background/font-weight/width still wins the cascade unchanged (verified, not just assumed); added `.sap`/`.approved-ro`/`.pending-readonly`/`.zero` (kept LAST so it overrides the other 3 on equal specificity) using existing tokens (`--status-sap`/`--status-approved`/`--ink`/`--paper-2`/`--ink-3`), backgrounds color-mix into `transparent` (not `--surface`) so point-2's row tint shows through. Headless Playwright verify (mocked API): 8/8 assertions PASS — SAP nonzero = green text+bg, Approved nonzero = blue text+bg, SAP-zero and board-zero both opacity 0.6 + transparent bg, editable pending input confirmed `<input class="month-value month-input">` NOT colored, 0 console errors. `npm test` 344/344 green (unchanged — testid-based assertions, text bubbles through the new span). `npm run lint` clean on touched files (pre-existing e2e/fixtures.ts warning unrelated). Screenshot saved (not read) to scratchpad `point3_month_value_colors.png` — awaiting jakkaritw visual review.
- [x] **GL `edit_by` admin-only lock — design v2 built, flag OFF (2026-07-17)**: `Settings.gl_edit_by_enabled: bool = False` (`backend/app/config.py`) gates everything; NEW `backend/app/gl_access.py` (`normalize_edit_by`/`fetch_admin_gl_codes`/`is_admin_only_gl`/`department_has_pending_admin_gl_rows`) shared by all 4 call sites so they can never disagree on what counts as an admin GL. **Rule 1 (secret from non-admin+approvers):** `reference_data.fetch_gl_accounts` drops an admin-GL row ENTIRELY for a non-admin caller (`routers/reference.py` only resolves scope when the flag is on); `read_model.merge_budget_rows`/`get_budget_grid` strip admin-GL rows for any non-admin caller (See-only/approver included) via one extra query only when flag-on-and-not-admin; `routers/subform.py`'s `GET /budget/detail` gets a cheap defensive `is_admin_only_gl` 403 guard (unreachable in practice — admin GLs are never one of the 6 special groups, confirmed no data leak possible there even without it). No server-computed total/summary exists beyond per-row `total_year` (confirmed by inspection) — row-strip alone covers rule 1 fully. **Rule 4 (403 on direct write):** `write_model._derive_dim_snapshot`/`_lookup_gl_group` optionally resolve `edit_by`; new `AdminOnlyGlError`→`admin_only_gl`/403 raised in both `_save_one_pending_row` and `_save_one_detail_line` right after dims resolve. **Rule 3 (admin-GL rows can't enter the normal approval chain):** `approval.submit_department` — new `AdminGlInNormalSubmitError`→`admin_gl_in_normal_submit`/403, raised ONLY when an admin who also Fills the department (`is_filler` branch) tries a normal submit while the department has pending admin-GL rows (`department_has_pending_admin_gl_rows`); **scoped literally to "an admin submits normally" per the approved brief — a non-admin filler is NOT covered by this guard** (flagged as a spec-literal choice, not silently broadened, since only admin can ever write an admin-GL row in the first place per rule 4). **Frontend (additive only):** `api/types.ts` `GlAccount.edit_by?: 'user'|'admin'`; `AddTransactionForm.tsx` appends "(เฉพาะแอดมิน)" to an admin-only GL's option label (only an admin ever receives that value) — no hiding logic needed, rows/reference lists already arrive pre-filtered. No frontend `messageForStatus` translation added for the 2 new 403s (kept the existing generic "ไม่มีสิทธิ์เข้าถึงข้อมูลนี้" message) — flagged as a deliberate scope-minimal choice, not an oversight. **Tests:** backend 596→642 mocked pass (+46, incl. 32 in new `test_gl_access.py`), 44→46 live-integration pass (+2, read-only, proves the REAL `dbo.gl_group.edit_by` — 13 admin/133 user/0 NULL — against `fetch_admin_gl_codes`/`fetch_gl_accounts`); frontend +1 vitest test in `AddTransactionForm.test.tsx` (346 total pre-existing at session start per the point-3 entry above; a parallel UI-parity agent's own uncommitted frontend churn landed the same day, not this feature's), `npm run build` + `oxlint` clean. **Pre-existing, unrelated flake found + isolated (not fixed, out of scope):** on THIS dev machine, `backend/.env` has `APP_ENV=local` + a `DEV_AUTH_EMAIL` — 19 "401 without auth header" router tests intermittently see a bypassed-auth 200 instead of 401 depending on `get_settings()`'s process-wide `@lru_cache` state/test-run timing; proven via an isolated `git worktree` of pristine HEAD (before ANY of this feature's changes) that the SAME 19 tests fail there too, deterministically — confirms this is a pre-existing local-env/test-order interaction, not a regression from this feature. Not touched (out of scope); flagged for whoever owns test infra. **NOT flipped ON** — jakkaritw flips `gl_edit_by_enabled` when ready to go live with the 13 real admin GLs. Not committed (per instructions).
- [x] **Lane 2.2 fix — admin-GL rows self-approved-on-save, `AdminGlInNormalSubmitError` guard REMOVED (2026-07-17, backend only, flag still OFF)**: the guard committed in `5119e19` was built on the wrong model (forced an admin-who-Fills into `_admin_direct_approve` whenever their department held a pending admin-GL row — stamping the WHOLE department APPROVED and bypassing the real approver on legitimate user-GL rows). Removed entirely: `backend/app/approval.py` — class `AdminGlInNormalSubmitError`, its `ERROR_HTTP_STATUS`/`ERROR_CODE_BY_EXCEPTION` entries, the guard block + GL-lock docstring paragraph in `submit_department`, the now-unused `settings` param + `Settings`/`get_settings` import; `backend/app/gl_access.py` — `department_has_pending_admin_gl_rows` (dead, only caller was the guard), docstring "4 call sites"→"3". `submit_department` now routes an admin-who-Fills straight to the normal chain, identical to a non-admin filler, regardless of admin-GL rows in the department (lane 2.2 rows are approved-on-save the instant admin saves them, ADR-0024, never enter `budget.approval_status` — no schema change, audited via existing `pending_budget._user`/`_updated_at`). **auto_submit exclusion (flag-gated, done now per jakkaritw's default):** `backend/jobs/auto_submit.py`'s `_find_true_draft_departments` "last editor" discovery now excludes admin-GL rows from BOTH the candidate row and the correlated MAX(`_updated_at`) subquery when `Settings.gl_edit_by_enabled` is True (`run()` fetches `gl_access.fetch_admin_gl_codes` only when the flag is on — flag OFF = byte-identical query, zero behavior change), so a true-DRAFT department's approver1 is never resolved from someone who only touched a secret admin-GL line. **NEW `docs/adr/0024-admin-gl-rows-self-approved-on-save.md`** (Accepted): defines lane 2.2 by `edit_by='admin'` only (disambiguated from ADR-0012's unrelated `template='ADMIN'` Template-2 door), narrows ADR-0011 ("only APPROVED→Gold" = lane-2.1/user-GL only; admin-GL flows to Gold unconditionally in Phase-2, not built), records the bug + removal rationale. **Tests:** `test_approval.py` — removed the 2 tests asserting the deleted guard's error/blocked-vs-allowed behavior, replaced with 2 tests proving BOTH an admin filler and a non-admin filler route straight to the normal chain (PENDING_APPROVER1, no error) regardless of admin-GL rows; `_flag_on()` helper + unused `Settings` import removed. `test_gl_access.py` — removed the 4 `department_has_pending_admin_gl_rows` tests + import symbol, module docstring updated. `test_jobs_auto_submit.py` — +4 new tests (2 proving the exclusion clause shape/params on `_find_true_draft_departments`, 2 proving `run()`'s flag-gating of `fetch_admin_gl_codes`). Full backend suite: 623→621 mocked pass (net -2, expected: -4 removed guard-behavior tests +4 new auto_submit tests -2 replaced approval tests), identical pre-existing 19 "401 without auth" flake before/after (confirmed byte-identical failing-test list, not a regression — see gotcha memory). Frontend untouched (confirmed no references to the removed error code). Flag still OFF — no live behavior change. Not committed (per instructions).
- [x] **UI-parity fix series, point 4 — GL Group chip (solid color per special group), 2026-07-17 (frontend only)**: `frontend/src/grid/model.ts` — new exported `groupChipClass(glGroup: string): string`, a verbatim port of the mockup's `GROUP_CHIP_CLASS` map (6 special groups → `gl-yellow/pink/purple/orange/blue/green`, `''` otherwise). **Gated on GROUP NAME, not `meta.is_special`** (lead's decision) — a GL can carry `is_special:false` while still in a chipped group (real fixture case: `model.test.ts`'s Travelling Expense GL), so group-name gating keeps every row in one group the same color. `frontend/src/grid/GridTable.tsx` — `gl-group-cell` renders `<span className="gl-chip special-gl-group {colorClass}">{gl_group}</span>` when `groupChipClass()` is non-empty, else plain text unchanged. `frontend/src/styles/global.css` — chip CSS ported verbatim (exact hex/glow values) scoped `.gl-group-cell .gl-chip.special-gl-group` (base) + `.gl-group-cell .gl-chip.{color}` (6 color modifiers). Headless Playwright verify (mocked API, mixed rows): 4/4 PASS — Entertainment chip bg `rgb(242,183,5)`, Training & Seminar chip bg `rgb(59,130,246)`, Office expenses (non-mapped) has NO chip span (plain text), 0 console errors; chip's box-shadow glow NOT clipped (`.gl-group-cell` computed `overflow: visible`). `npm test` 346/346 green (344→346: +2 new `groupChipClass` tests in `model.test.ts`; existing GridTable tests unaffected, none asserted the plain-text DOM structure). `npm run lint` clean on touched files (pre-existing e2e/fixtures.ts + approver-journey.spec.ts warnings unrelated, not touched). Screenshot saved (not read) to scratchpad `point4_gl_chip.png` — awaiting jakkaritw visual review.
- [x] **UI-parity fix series, point 6 — 3-dot status legend in the grid toolbar (2026-07-17, frontend only)**: `frontend/src/grid/BudgetGrid.tsx` — `.grid-toolbar` gained a `data-testid="status-legend"` block (placed at the END of the toolbar row, after `AdminModeToggle` — this toolbar has no spacer/actions split like the mockup's) with 3 `.legend-item`s (sap/approved/pending dots), verbatim port of mockup `0002.3budget-export.html` markup lines 1097-1101. **Year labels are load-bearing** (why users were confused today): SAP/Approved show `year - 1` (board/SAP standing-year baseline per `read_model.py:383`), Pending shows `year` (the selected planning year) — mirrors mockup JS lines 2145-2147 (`standY`/`standY+1`). `frontend/src/styles/global.css` — `.legend`/`.legend-item`/`.legend-dot{.sap,.approved,.pending}` ported verbatim from mockup lines 367-373, placed right after `.year-select` in the toolbar CSS block, reusing existing `--status-sap/approved/pending` tokens. TDD: 1 new RED-first vitest test (`BudgetGrid.test.tsx`) asserting 3 items + exact year-labeled text at year=2027 → SAP/Approved (2026), Pending (2027); 348/348 vitest pass (was 346, +2: legend test + a concurrently-landed unrelated test), `npx oxlint` clean on touched files. Headless Playwright verify (real Chromium, mocked routes, live dev server 127.0.0.1:5180, year select set to 2027): 9/9 PASS — 3 legend-items, all 3 dot computed backgrounds match resolved `--status-sap/approved/pending` exactly, all 3 label texts match the expected year-shifted strings verbatim, 0 console errors. Screenshot saved (not read) to scratchpad `point6_legend.png` — awaiting jakkaritw visual review. STRICT SCOPE respected: no changes to GL chips/title/status-cell swatch/header rows/filters/resize/buttons-selects/admin zone/points 1-5 code.
- [x] **UI-parity fix series, point 6 follow-up — year dropdown reads as standing year, not planning year (2026-07-17, frontend only)**: `frontend/src/grid/YearPicker.tsx` — option `value` stays the planning year (unchanged `onChange`/state/API contract), label changed to `Year {y - 1}` (standing year) to match mockup `0002.3budget-export.html` semantic and line up with the point-6 legend (`year-1`/`year-1`/`year`, untouched). aria-label changed `"ปีงบประมาณ (Pending)"` → `"ปีฐาน (SAP/Approved · Pending = ปีถัดไป)"`; doc comment updated. `frontend/src/App.test.tsx:64,109` — updated `getByRole('combobox', { name: /ปีงบประมาณ/ })` → `/ปีฐาน/` (aria-label changed). `frontend/src/grid/YearPicker.test.tsx` — added 1 assertion: option `value=2027` renders label `"Year 2026"`. No backend/contract/legend changes. `npm test` 349/349 green (was 348, +1). `npx oxlint` clean on touched files. Headless Playwright (real Chromium, mocked routes, live dev server 127.0.0.1:5180): selecting the option labeled "Year 2026" → `/budget` request fires with `year=2027`; legend shows `SAP (2026)` / `Approved (2026)` / `Pending (2027)`; 0 console errors — PASS. Screenshot saved (not read) to `%TEMP%\yearpicker_verify.png` — awaiting jakkaritw visual review.
- [x] **UI-parity fix series, point 7 — 7px color swatch before each status label (2026-07-18, frontend CSS only)**: `frontend/src/styles/global.css` (~lines 866-901) — `.status-cell` ported from mockup `0002.3budget-export.html` lines 459-466 (`display:flex;align-items:center;gap:8px`, letter-spacing 0.03em→0.04em, base `color:var(--ink-3)`) + new `.status-cell::before{content:'';width:7px;height:7px;flex-shrink:0;background:currentColor}` (square swatch, no border-radius — distinct from point-6's round legend dots). **Critical interaction handled** (not in the mockup snippet verbatim, needed for the React-only pending-cell structure): added `flex-wrap:wrap` to `.status-cell` + `flex-basis:100%` to both `.special-hint` and `.reference-hint` so they wrap to their own full-width line under the swatch+label row instead of sitting inline; `.special-open-btn` untouched, stays a normal (non-full-width) flex item next to the label. `white-space:nowrap` kept (verified it does not block flex-wrap item-wrapping, only same-item text wrapping). `GridTable.tsx` NOT touched — CSS-only fix. Headless Playwright verify (mocked API, live dev server 127.0.0.1:5180): 17/17 PASS — sap/approved/pending swatches each 7x7px, border-radius 0 (square), background == the resolved `--status-{sap,approved,pending}` token via currentColor; all 3 `.status-cell` computed `display:flex`; a See-only Entertainment row's `.special-hint` wraps below the swatch+label row (`hintTop` 643.45 vs `statusCellTop` 594.70, i.e. on its own line, not inline) — `.reference-hint` shares the identical CSS rule so not independently re-tested; 0 console errors. `npm test` 349/349 green (unchanged, CSS-only). `npm run lint` clean on touched files (pre-existing e2e/fixtures.ts + approver-journey.spec.ts warnings unrelated). Screenshot saved (not read) to scratchpad `point7_status_swatch.png` — awaiting jakkaritw visual review. STRICT SCOPE respected: no changes to legend/title/GL chips/month colors/freeze/header/buttons/selects/admin zone.
- [x] **UI-parity fix series, point 8a — grid header: group-head row + real month names + now-month highlight (2026-07-18, frontend only)**: `frontend/src/grid/model.ts` — new `MONTH_LABELS: Record<MonthKey,string>` ('m01'→'Jan'…'m12'→'Dec', mockup `MONTHS_EN`) + `nowMonthKey(date = new Date())` (takes an optional `Date` for testability, real clock by default). `frontend/src/grid/GridTable.tsx` — thead now renders 2 sticky rows: NEW `tr.group-head-row` first (merged `colSpan=3` over the 3 frozen identity cols + a blank `<th>` over Status + `colSpan=12` over the months, labeled with a NEUTRAL serif "งบประมาณรายเดือน (บาท)" — deliberately NOT a bare year, since the month columns mix SAP/Approved (year-1) with Pending (year), the exact confusion 2 prior sessions already hit), then the existing header row (`tr.col-row`) — Cost Center/GL Code/GL Group/Status labels + month `<th>`s wrapped in `.th-label` spans, header no longer shows raw `m01`..`m12`. `MonthCells`/`PendingCells`/`TxnBlock` gained a `nowMonth: MonthKey` prop threaded from `GridTable` so the current month's `<td class="month-cell now">` is marked across all 3 layers (sap/board/pending), matching the highlighted `<th class="month-col now">`. `frontend/src/styles/global.css` — ported mockup lines 392-400/410/438-448 verbatim: `.th-label{display:block;line-height:1.2}`, `tr.group-head-row th` (sticky top:0 z-index:11 height:22px mono/uppercase/9.5px), `tr.group-head-row th.frz{z-index:17}`, NEW `.month-group-label` override (serif/16px/normal-case, not in the mockup snippet — needed since our label is prose text, not a bare year), `tr.col-row th{top:22px}` (stacks below the 22px group-head), `th.month-col .th-label{font-size:14px;font-weight:700}`, `.month-col.now`/`.month-cell.now` (gold-dot + accent-underline + tinted bg, `--c-gold` already existed in `tokens.css` — no new token needed). **Design decision flagged**: the group-head row's identity-merge cell got `frz-edge` added (box-shadow scroll cue) beyond the task's literal `"frz frz-1"` spec, matching the existing project convention for every other 3-col-merge-adjacent-to-Status cell (`TxnBlock`'s approved/pending rows, `SubtotalRow`) — kept for visual consistency, not scope creep. Headless Playwright verify (mocked-free, live dev server 127.0.0.1:5180 against real backend data): 12/12 PASS — Jan/Dec real labels (not m01/m12), group-head-row first with colSpan=12 month-group cell, both header rows computed `position:sticky` (col-row `top:22px`), exactly ONE `.month-col.now` per side-table (COST + SG&A each have their own full header — this grid renders 2 separate tables, not 1, so "exactly one" is correctly scoped per-table) at index `new Date().getMonth()` with a distinct background, matching `.month-cell.now` body cells exist, `.th-label` font-size 14px — 0 console errors. `npm test` 353/353 green (349 + 4 new: 3 `nowMonthKey`/`MONTH_LABELS` unit tests in `model.test.ts` + 1 new GridTable header/now-month assertion). `npm run lint` clean on touched files (pre-existing e2e/fixtures.ts + approver-journey.spec.ts warnings unrelated, not touched). Screenshot saved (not read) to scratchpad `point8a_group_head.png` — awaiting jakkaritw visual review. STRICT SCOPE respected per brief: 8b (column filter inputs) and 8c (resize/reset) NOT done; no changes to legend/title/GL chips/row tint/status swatch/freeze offsets/buttons/selects/admin zone.
- [x] **UI-parity fix series, point 8b — per-column filter inputs (CC/GL Code/GL Group), 2026-07-18 (frontend only)**: `frontend/src/grid/model.ts` — new `ColumnFilters` type + `BLANK_COLUMN_FILTERS` + `filterRows(rows, glRef, filters)` (pure, case-insensitive substring match on `cost_center`/`gl_account`/the RESOLVED `gl_group` via `glMetaFor` — never a field on `BudgetRow` itself; empty filter = matches all; runs BEFORE `groupAndSortBySide` so both side-tables + their subtotals reflect only matching rows). `frontend/src/grid/GridTable.tsx` — `colFilters` state held LOCALLY (`useState`, not lifted to `BudgetGrid`) since both COST/SGA side-tables live inside this one component; each table's `col-row` renders the SAME 3 bound inputs (`data-testid="filter-cc"|"filter-gl"|"filter-glgroup"`, Thai placeholder "กรอง…") inside the 3 frozen identity `<th>`s, so typing in either table's input updates the ONE shared state and re-filters both. Status `<th>` and each of the 12 month `<th>`s get a `.col-filter-spacer` (equal-height header cells). **Empty-result handling**: unfiltered-empty (`rows.length===0`, no data at all) is UNCHANGED — plain `grid-empty` div, no headers (nothing to filter). A side that legitimately never has any rows (pre-filter) still renders nothing, same as before. A side WITH data whose FILTERED set is zero keeps its header (filters stay visible/editable) and shows "ไม่มีรายการที่ตรงกับตัวกรอง" in the body instead of transaction rows (bottom "รวมทั้งหมด" subtotal is skipped in that case — an all-zero total row would be confusing clutter). `frontend/src/styles/global.css` — `.col-filter`/`.col-filter-spacer`/`.grid-empty-row` ported verbatim from mockup lines 411-420, placed after the existing `col-row th{top:22px}` rule. **Tests**: `model.test.ts` +6 (`filterRows` — passthrough on blank filters, cc/gl/gl_group substring match, AND-combination across columns, zero-match); `GridTable.test.tsx` +6 (filter inputs render, CC filter narrows txn-blocks, gl_group filter + clear restores, subtotal recomputes for the filtered set, zero-match keeps the input editable + shows the empty message, one shared filter narrows BOTH side-tables at once, `.col-filter-spacer` present under Status + all 12 months). `npm test` 353→365 green (all new, no regressions). `npx tsc -b --noEmit` clean; `npx oxlint` clean on touched files (pre-existing e2e/fixtures.ts + approver-journey.spec.ts warnings unrelated). Headless Playwright verify (mocked routes, live dev server 127.0.0.1:5180, 2 cost centers × 2 gl groups × both COST+SGA sides): **11/11 PASS** — 3 filter inputs exist; CC filter "North" 3→2 txn-blocks (only CCA-North remain); gl_group filter "Entertainment" → 1 matching row, clearing restores 3; shared filter narrows COST+SGA together (both showed only CCA-North rows); zero-match filter shows the empty message AND the `filter-cc` input stayed present+editable (value preserved, then cleared to restore all 3 rows); 13 `.col-filter-spacer`s found (Status + 12 months) in the COST table's col-row; 0 console errors. Screenshot saved (not read) to scratchpad `point8b_col_filters.png` — awaiting jakkaritw visual review. STRICT SCOPE respected: 8c (column resize / Reset-columns button) NOT done; no changes to group-head row/month names/now-highlight (8a)/legend/title/GL chips/month colors/row tint/freeze offsets/status swatch/buttons/selects/admin zone.
- [x] **UI-parity fix series, point 8c — column resize (drag) + Reset columns, LAST sub-part of point 8 (2026-07-18, frontend only)**: `frontend/src/grid/model.ts` — new `ColumnWidths{cc,gl,glGroup}` state shape + `DEFAULT_COLUMN_WIDTHS` (130/150/150, matches point-1's old static values) + `clampColumnWidth` (60-800, mockup `initColumnResize` verbatim) + `freezeOffsets(widths)` (derives `frz1=0/frz2=cc/frz3=cc+gl` with NO DOM measurement — replaces the mockup's imperative `applyFreeze()`, a deliberate React-idiomatic deviation per the brief) + `loadStoredColumnWidths`/`persistColumnWidths` (localStorage `budgetGridColWidths`, guarded try/catch end-to-end — corrupted JSON/quota-exceeded/disabled storage all fall back to defaults per-key, never throws). `frontend/src/grid/GridTable.tsx` — `colWidths` state held LOCALLY (same reasoning as 8b's `colFilters`: both COST/SGA side-tables live in this one component and must share ONE width set to stay pixel-aligned; lazy-initialized from localStorage). Freeze offsets applied as inline `style={{'--frz1','--frz2','--frz3'}}` on EACH `.data-table` (both tables read the same computed object). 3 identity `<th>`s get `style={{width,minWidth}}` from `colWidths` (table auto-layout propagates to the whole column incl. colSpan merges below). Drag: `.col-resize` handle per identity th (both tables, 6 total) — `dragStateRef` (math source of truth, no re-render per frame) + `dragListenersRef` (exact function refs, so mouseup AND unmount both remove the SAME listeners — no leak either way, regression-tested); `document.body.classList` toggles `col-dragging`; mouse+touch both wired; width persisted to localStorage only on drag-END (not every frame). Reset: small `.grid-column-controls` right-aligned ghost button ABOVE `.grid-sides` (not lifted to `BudgetGrid`'s toolbar — colWidths is local to GridTable, simpler per the brief's stated alternative; `BudgetGrid.tsx` untouched, confirmed 0 diff) — `onClick` resets state + localStorage to defaults. `frontend/src/styles/global.css` — `.btn-sm`/`.btn-ghost` (new, mockup lines 166-168 verbatim) + `.grid-column-controls` + `.col-resize`/`::after`/`.is-dragging`/`body.col-dragging` (mockup lines 422-435 verbatim) + removed the STATIC `--frz1/2/3` from `.data-table` (superseded by inline React state). **1 real bug found + fixed during verification (not in the mockup port, a genuine CSS stacking-context issue, confirmed present in the mockup HTML too — not introduced by this port)**: each `.col-resize` handle spills ~3px into the NEXT identity column by design (`right:-3px`, mockup-verbatim) — but `th.frz` already establishes its OWN stacking context (`position:sticky`+`z-index`), so the handle's `z-index:20` only competes within its OWN th and can never paint above a SIBLING th; real mousedown/Playwright `elementFromPoint` on the right half of every handle resolved to the NEXT column's th instead, silently swallowing the click (drag never starts) — proven live via `elementFromPoint` probing (offsets 4-6.5px of the 7px handle hit `frz frz-2`, not `col-resize`). Fixed with 2 new rules `tr.col-row th.frz-1{z-index:18}` / `th.frz-2{z-index:17}` (frz-3 needs no bump, its neighbor Status th is already z10 < 16) — placed after the shared `th.frz{z-index:16}` rule to win by source order; verified the full 7px handle is clickable end-to-end after the fix. **Tests**: `model.test.ts` +14 (`clampColumnWidth`/`freezeOffsets`/`loadStoredColumnWidths`/`persistColumnWidths` incl. corrupted/out-of-range/missing-field/quota-exceeded guards); `GridTable.test.tsx` +7 (handles render on both tables, drag widens + updates `--frz2`/`--frz3` + keeps both tables aligned, clamp caps 800/floors 60, Reset restores defaults incl. `--frz2`, `is-dragging` class scoped to only the active handle, `body.col-dragging` toggles, NO leaked window listeners after unmount mid-drag). `npm test` 365→383 green (was 365, all new, no regressions), `npm run build` (tsc -b + vite) clean, `npm run lint` clean on touched files (pre-existing e2e/fixtures.ts + approver-journey.spec.ts warnings unrelated, not touched). Headless Playwright verify (real Chromium, live dev server 127.0.0.1:5180, real backend data): **15/15 PASS** — 3 handles × 2 tables; drag CC +50px → 130→180px + `--frz2`=180px + `--frz3`=330px, BOTH tables (COST/SGA) stay aligned on width AND `--frz2`; Reset → 130px/`--frz2`=130px; huge positive drag clamps to 800px (state AND rendered, wide enough not to hit any content floor); huge negative drag clamps the STATE to 60px (`style.width`, authoritative — matches the unit-tested `clampColumnWidth`) though the browser's RENDERED `getBoundingClientRect()` can legitimately stay wider (~99px observed) when the real Cost Center code text ("10IT011200") in the data cells can't shrink that far — only the HEADER label has ellipsis truncation, the DATA cells deliberately never truncate real values (same in the mockup source, not a regression, not fixed — truncating real CC codes would be a worse UX than a resize floor that's a few px higher than requested in extreme cases); 0 console errors. Screenshot saved (not read) to `%TEMP%\point8c_col_resize.png` — awaiting jakkaritw visual review. localStorage persistence: YES (built, guarded, tested). **UI-parity fix series (points 1-8) is now COMPLETE** — no more sub-parts of point 8 remain; STRICT SCOPE respected throughout: no changes to 8b filter logic/8a header/row tint/month colors/GL chips/title/legend/status swatch/buttons(pt9)/selects(pt10)/admin zone(pt11).
- [x] **Grid column polish — status-cell white band + fit-to-content column widths (2026-07-18, frontend only, 2 user-requested refinements on top of point 2 + point 8c)**: **(1) Status column joins the white frozen band** — `frontend/src/styles/global.css` — the 3 point-2 row-tint selectors gained `:not(.status-cell)` (now `td:not(.shared):not(.frz):not(.status-cell)`), and `.status-cell` gained an explicit `background: var(--surface)` (was relying on incidental transparency showing the table's own bg through — made explicit so it actually equals the frozen band's own background, not just looks like it); colored text/7px swatch (point 7) untouched, only the cell's own background excluded. **(2) Identity columns default to FIT-TO-CONTENT, not hardcoded 130/150/150** — `frontend/src/grid/model.ts`: `fitColumnWidth(rawTextWidth)` (padding allowance `COLUMN_WIDTH_MEASURE_PADDING=24` + existing `clampColumnWidth` 60-800), `selectMeasureCandidates(rows, glRef, cap=30)` (dedup + longest-N per column, unbounded for glGroup — small fixed cardinality), `hasStoredColumnWidthsOverride`/`clearStoredColumnWidths` (localStorage presence-check + removal, guarded); `DEFAULT_COLUMN_WIDTHS` (130/150/150) demoted to "pre-measurement placeholder only" (docstring updated), no longer "the" default. `frontend/src/grid/GridTable.tsx`: new hidden `<ColumnWidthMeasurer>` (off-screen, `visibility:hidden` not `display:none`, real classNames `idx-cell`/`gl-code-text`/`gl-chip` so font/weight match) + `measureColumnWidths()` (DOM `getBoundingClientRect` max per column via `[data-measure-col]`) + a `useLayoutEffect` (deps `[rows, glRef]`, NEVER `colWidths` — no loop) that applies the fitted default UNLESS `hasOverrideRef` is true (a saved localStorage width OR a manual drag this session — checked once at mount, set permanently on drag-start); guards `rows.length===0` (keep last-good widths, no floor-collapse). Reset-columns now clears BOTH the override flag and localStorage, then re-measures synchronously (button tooltip updated) — "reset" means "back to fit", not "back to 130/150/150". Both side-tables (COST/SGA) stay pixel-aligned by construction (same measurement pass, same state). **Tests**: `model.test.ts` +17 (`fitColumnWidth`/`selectMeasureCandidates`/`hasStoredColumnWidthsOverride`/`clearStoredColumnWidths`), `GridTable.test.tsx` — rewrote the 2 tests that hardcoded 130-based drag math to read the fitted starting width dynamically first (jsdom deterministically floors every column to `COLUMN_WIDTH_MIN`=60 since it never lays out real text — a real browser measures actual content), +3 new tests (default is content-fitted/<130/both-tables-equal/frz-tracking, a stored localStorage width wins on mount, a manual drag survives a later `rows` prop change without being clobbered). `npm test` 383→398 green (all new/updated, no regressions), `npx tsc -b` clean, `npm run lint` clean on touched files (pre-existing e2e/fixtures.ts + approver-journey.spec.ts warnings unrelated). Headless Playwright verify (real Chromium, live dev server 127.0.0.1:5180, real backend data, Digital Technology user): **12/12 PASS** — status-cell computed bg (`rgb(251,249,243)`) exactly equals the frozen-band td's bg, month-cell bg differs (still tinted); real CC column measured to 104px (< the old 130px, >= the 60px floor), COST/SGA tables' CC widths equal, `--frz2`=104px/`--frz3`=199px (104+gl 95) both tracking; drag still widens (+60px→164px, `--frz2` tracks), Reset re-measures back to 104px (not a hardcoded constant) and clears localStorage; swatch/colored-text spot-check confirmed unaffected (SAP text `rgb(94,122,80)`, swatch 7×7px same color); 0 console errors. Screenshot saved (not read) to scratchpad `point8d_grid_column_polish.png` — awaiting jakkaritw visual review. STRICT SCOPE respected: `.btn` block (uncommitted point-9) untouched; no changes to title/legend/GL chips/month layer colors/status swatch shape/filters logic/now-highlight/selects/admin zone. Not committed (per instructions).
- [x] **UI-parity fix series, point 11 (FINAL) — admin-only "Approved · Admin" read-only info strip (2026-07-18, frontend only)**: `frontend/src/grid/BudgetGrid.tsx` — new `{scope.isAdmin && (<div className="admin-zone" data-testid="admin-zone">...)}` block, ported verbatim from mockup `0002.3budget-export.html` markup lines 1123-1136 (gear SVG, title "งบอนุมัติ (Approved) · Admin", ⚠ scope-independence note, Master FX display + "แก้ที่ Master Currency ↗" link to the master-tables SWA `master-currency.html`, SharePoint provenance note) — placed right after the `grid-toolbar` div and before `ApprovalActionBar`/error/loading/`GridTable`, i.e. above the table, matching the mockup's own ordering (toolbar → admin-zone → table-panel). FX value is the literal placeholder "—" (no FX data wired yet — honest, not fabricated); FX year = `year - 1`, same convention as the existing SAP/Approved legend items (board_year). `frontend/src/styles/global.css` — `.admin-zone`/`.admin-zone-head`/`.admin-zone-ic`/`.admin-zone-title`/`.admin-zone-note`/`.admin-zone-actions` ported verbatim from mockup CSS lines 334-347, inserted after the `.legend-dot` rules. Read-only by design: no buttons, no import/export. **Tests**: `BudgetGrid.test.tsx` +2 (admin scope shows the strip with exact title/FX-year/read-only/link/SharePoint text and zero `<button>`s inside; non-admin scope never renders `.admin-zone`). `npm test` 398→400 green, no regressions; `npx tsc -b` clean; `npm run lint` clean on touched files (pre-existing e2e/fixtures.ts + approver-journey.spec.ts warnings unrelated, not touched). Headless Playwright verify (mocked `/scope` route to force `is_admin:true`/`false` — the live dev-server user is not an admin — real Chromium, live dev server 127.0.0.1:5180): **14/14 PASS** — exactly one `.admin-zone` for admin scope, title text exact match, FX span contains "—" and "read-only", Master Currency link `target=_blank` + correct SWA href, SharePoint note text present, 0 `<button>`s inside, `border-left` computed 4px, background tint differs from plain `--surface` (`oklab(...)` vs `rgb(251,249,243)`), FX year label = selected-year−1 (`FY2026` for planning year 2027), `.admin-zone` absent for non-admin scope, 0 console errors both scopes. Screenshot saved (not read) to scratchpad `point11_admin_zone.png` — awaiting jakkaritw visual review. STRICT SCOPE respected: no import/export button, no real FX data wired, grid table/freeze/filters/resize/other points untouched, non-admin rendering unchanged. **UI-parity fix series (points 1-11) now fully covers the brief** — points 9/10 (buttons/selects) were committed separately by a prior session but have no corresponding plan.md entry (pre-existing gap, not created by this task — flagging for whoever next syncs plan.md against `git log`). Not committed (per instructions).

## Master-tables → Excel migration (ADR-0018/0019, grilled 2026-07-11)
- [ ] design + build the Excel(SharePoint)→Fabric sync job (`cman-dw-ws` / `modern_lh_cman_dw`) — cadence, validation-on-ingest, skip-blank-Filler-row tolerance (02-data-modeler + 04-data-engineer). Land against the DW project's existing "SharePoint CSV" master lane (19.dw_jakkaritw ADR-0020, `cc-dept` already queued) — but that lane reads CSV only + has no per-cell Excel reader / no cross-row aggregation, so 2 gaps to fill: (a) Excel reader, (b) UNION-Filler aggregation
- [ ] **master_currency = Excel on SharePoint** (ADR-0018 dataset #4, file `อัตราแลกเปลี่ยนเฉลี่ยรายปี.xlsx`, cols `year | average rate usdthb` → `fiscal_year`/`usd_thb`; admin-maintained, values change freely). Sync must: coerce rate text→number (don't trust Excel cell type), validate year=4-digit-int & rate>0 (WARN/skip bad rows), and the app FAILS LOUD if the planning year's rate row is missing (never a silent 35.00 fallback). Supersedes ADR-0015's cfg_master/Module-09 web-edit plan
- [ ] once sync is live and proven: decommission `03-edit-master-table/master-tables/01frontend/{gl-group,orgcode-costcenter,hide-document}.html` + their backend modules
- [ ] open question carried from ADR-0019: does the direct-manager See-scope lookup need Primary+Acting posstatus (like the old orgcode lookup did)?

---

## Done (archive)

Completed milestones — one line each. Detail lives in git history.

- Docs consolidation (PR #1 / commit 3800e32, 2026-06-14) — CLAUDE.md slimmed (~1300→305 lines, points to CONTEXT/ADR/reference); README rewritten onboarding (~619→182); project-context deduped; reference payload → `docs/reference/` (approval-workflow, budget-templates, data-platform-map, data-sources, gl-master); stale Azure SQL/Streamlit/ACR scrubbed.
- Fabric SQL migration code path (ADR-0017) — sync_employees + daily workflow on Fabric SP; Azure SQL helper archived.
- 0007 Orgcode↔CostCenter frontend fix — CC multi-select, API-wired list/save/delete, Lakehouse cost-center reference. Code-review fixes applied (409 on dup, event delegation/XSS, stale handlers removed).
- gl-group.html wired to real backend (`/api/master/gl-group/*`, Export CSV) — code review APPROVE.
- master-currency.html created from mockup (4th master-edit page); nav hrefs repointed across all pages.
- Budget transactional data model — `docs/specs/budget-transactional-data-model.md` (7 `budget.*` tables + refs, managerempcode chain).
- Spec reconciled to post-dating decisions (2026-07-12): approval unit → (ฝ่าย, fiscal_year) per ADR-0008; RLS → CC↔Filler map per ADR-0019; SAP actuals → DW `fact_gl_trans` read-through (**new ADR-0020**); board_budget → yearly Excel SharePoint drop, 14-col A–N, year-from-filename (**new ADR-0021**); CONTEXT.md glossary fixed; gate 06 passed after 1 blocker fix. Real-data verification: cc dept.xlsx = 210 CC / 114 ฝ่าย, 0 orphan, 0 multi-ฝ่าย; approved_budget file structure confirmed.
- Sign-off specs (MS Word) built/rebuilt: main web app (v0.6), GL Group, master currency, web-access/submit (module 10) — all re-pointed to canonical mockup `0002.1budget-export.html`, validators green.
- SpecC (Master Tables) sign-off doc clarity polish (2026-06-25, version2/) — wording-only across all 5 modules via run-id OOXML edits (no install): colon-chains→bullets, tightened Context/Downstream/FX-impact prose, consistent phrasing; all facts/numbers/①–⑤/ADR/screenshots preserved (deterministic token check, 0 dropped). Per-module changelog+version bump (GL v0.2.2 · Closing v0.2.1 · OrgCC/Hide v0.3.1 · Currency v0.4.1). User added own `(L3/L4/L2 3 คน)` edit on the candidate, kept. Reusable tools: `tasks/signoff-spec-v2/_tools/{extract_runs,apply_runs,check_preservation,build_clarity_edits}.py`.
- SpecB (GL Subform) sign-off doc clarity polish (2026-06-26, version2/, commit 758d320 via **CC→Cursor handoff**) — SpecB is run-fragmented (no clean single-run paragraphs), so used a **paragraph-collapse** method (rebuild one run per paragraph, rPr preserves Thai `cs`+Latin `ascii` fonts). CC pre-built+proved tools `tasks/signoff-spec-v2/_tools/{extract_paras,apply_paras,check_paras}.py`; cs rewrote 40 paragraphs (colon-chains→bullets), version→v0.4.1 + changelog. Validate PASS: check_paras 0 dropped/0 wrong-index, images 14=14 untouched, markers/facts intact, commit scope clean.
- Repo cleanup (2026-06-26, commit 1a2d3fc via **CC→Cursor handoff**) — user-approved exact-list delete: temp junk (`tasks/_tmp_*`), 6 superseded one-off SpecB scripts, `bin/` verify throwaway (~1.8MB, gitignored); `git rm` 4 done v2 tools + `edits_specA.json`; `git rm` `version2/` SpecA/B/C docx (live on SharePoint, in git history). Committed stray CURSOR_PROMPT.md. Kept 9 reusable `_tools` + task records. Validate PASS: commit scope clean (only cleanup paths), pre-existing unrelated changes + `index.html` untouched.
- ADRs 0001–0017 written (RLS, see/fill/approval-unit, admin override/toggle, FX snapshot, Fabric SQL DB).
- **Fix: production `HYT00 Login timeout expired` on Fabric SQL connect (2026-07-16)** — root cause: in the ACA Linux container, ODBC driver 17/18's internal `Authentication=ActiveDirectoryServicePrincipal` token flow stalls ~20.3s, exceeding pyodbc's 15s default login timeout (TCP/TDS/TLS itself is sub-second; a direct HTTPS call to login.microsoftonline.com is 0.12s). Fix: `backend/app/db.py` now acquires the AAD token in Python via `msal` (client-credentials, cached `ConfidentialClientApplication` per client_id) and injects it via the ODBC `SQL_COPT_SS_ACCESS_TOKEN` (1256) `attrs_before` attribute, `timeout=30` as a safety net; conn string no longer carries `Authentication=`/`UID=`/`PWD=`. `_ODBC_DRIVER` constant and `backend/Dockerfile` untouched (out of scope). `msal==1.36.0` pinned in `backend/requirements.txt`. TDD: `backend/tests/test_db.py` rewritten (11 tests: token packing, attrs_before/timeout injection, conn-string no longer has legacy auth keys, msal-failure→RuntimeError with error_description, msal app cached across calls) — full backend suite 506 passed / 44 deselected (integration, needs live DB).
- **Gate follow-ups on the above fix (2026-07-16, APPROVE-WITH-SUGGESTIONS)** — `backend/app/routers/health.py`: `deep=1` now also catches `RuntimeError` (msal token failure) alongside `pyodbc.Error`, same `logger.exception` + degrade-to-`{"db":"fail"}` style, no secret leak (+1 test). `backend/app/db.py`: msal app cache now keyed by `(tenant_id, client_id)` not `client_id` alone, so a reused client_id across tenants can't reuse the wrong tenant's cached token (+1 test). Full backend suite: 508 passed / 44 deselected.
- master-tables `01frontend/index.html` rebuilt as a real Home/overview grid (2026-07-06) — replaces the old `<meta refresh>` redirect stub with 5 module cards (GL Group/Orgcode-CC/Hide Document/Master Currency/Closing Date), reusing budget-closing-date.html's design system verbatim (theme, fonts, nav). Playwright headless verify PASS (13/13 assertions: 5 cards, hrefs, module badges 03/07/08/09/10, downstream tags, nav active state, 0 console errors, theme toggle). No backend/data wiring — static presentational page only.
- [x] **B6 — staging perf measurement done 2026-07-17 (read-only, no code/scale changes)**: Container App `cman-budget-web-stg` — `minReplicas=0`/`maxReplicas=2` (scales to zero, cold starts DO happen), 0.5 CPU/1Gi per replica, no scale rules override (platform default), no container-level probes configured. Warm baseline (filler `anurakb@chememan.com`, header-spoofed): `/health` 35-40ms (first hit 121ms, TLS/keep-alive warmup), `/health?deep=1` (real msal+pyodbc round-trip) 133-187ms, `GET /budget?year=2027` (real full-year grid load, no dept filter = what the frontend does on first paint) **787-1207ms, median 908ms**, payload 18.6KB/26 rows for this one filler's See-scope. **Real finding, not previously flagged**: `read_model.get_budget_grid` → `sap.fetch_sap_actuals` runs a company-wide `GROUP BY` over `gold.fact_gl_trans` (23.7M raw rows) for the WHOLE fiscal year on **every single grid request regardless of caller's RLS scope or cost_center/department filter** — the CC restriction is pushed into the local board/pending join SQL but NOT into the SAP read-through query, so the ~900ms-1.2s server time above is paid by every filler even though their own payload is tiny; an admin-wide/admin_view_enabled request (untested — no admin creds in scope) would return a much larger payload (est. 1,700+ keys × ~730 bytes/row ≈ 1.2MB+) built from the same expensive scan, likely multi-second. Cold-start: **ESTIMATED, not measured** (replica had been running only ~4 min when tested, warm — forcing a real scale-to-zero would need a 15+ min idle wait, out of scope per task); reasoning from config: msodbcsql17 layer + python:3.12-slim-bookworm image, ACA consumption-plan scale-from-zero typically several seconds, plus the first real request pays the msal token acquisition (~0.1-0.3s, matches the HYT00-fix memory) + first pyodbc connect (~130-190ms, same order as the warm `/health?deep=1` figure above) — rough estimate 3-9s for the very first request after an idle period, then back to the warm baseline. **Verdict: acceptable for the ~273-user go-live as measured** (sub-1.5s single-CC grid load, sub-200ms health checks) but two follow-ups worth a decision before go-live: (1) confirm the admin-wide/admin_view_enabled grid load time with real admin creds (not tested here), (2) consider RLS-pushdown into `SAP_ACTUALS_SQL` (filter by cost_center IN scope) to cut the DW scan cost per-request instead of scanning the whole company every time — flag for whoever owns `app/sap.py` next, not fixed here per read-only task scope.
